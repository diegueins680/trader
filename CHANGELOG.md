## Unreleased
- Trading/Optimizer: normalize optimizer risk-sizing params so volatility stop/take-profit/trailing-stop multipliers must be positive, and sampled or prior-derived `riskPerTrade` is kept only with a fixed stop-loss, capped at the simulator's position-size scale hard limit, and dropped for volatility-stop trials whose realized stop fraction is data-dependent. This prevents optimizer trials and top-combo priors from emitting CLI-invalid or runaway risk sizing.
- Bot/Live Trading: add a tenant-level portfolio capital-preservation circuit breaker for live Binance futures bots. The breaker aggregates closed-trade returns across running bots and restored bot snapshots, trips on the existing capital-preservation loss-streak or rolling-loss thresholds, and blocks fresh entries for a 30-minute cooldown from the newest closed trade; reversal attempts reduce to flat and startup/autostart entries start flat while open-position adoption can still manage existing exposure. Closed-trade memory remains intact so outcome-weighted LSTM fine-tuning and adaptive filters continue learning from wins and losses.
- Bot/Live Trading: immediately fine-tune and persist the live LSTM after every newly closed trade using the updated closed-trade memory, so each realized win or loss affects the next entry decision instead of waiting for the next bar's normal online update. `bot.adjust` telemetry now reports the outcome weight and whether the immediate LSTM outcome update ran.
- Bot/Live Trading: learn combo-scoped close timing from each closed position's elapsed bars to maximum favorable PNL. Same-tenant bots running the corresponding combo now use supported live max-PNL timing evidence to shorten the effective max-hold horizon and exit with `MAX_PNL_TIMING`; runtime evidence never widens an existing max-hold cap.
- Web/Ops: enrich Binance account trades with interval-scored `maxPnl` and `maxPnlCloseTime` fields plus Max PNL / Max Close table and CSV columns. The backend pairs visible opening/closing lots, fetches per-symbol candle ranges, and scores the best gross PNL reachable from open time through `open + 2 * actual trade duration` using highs for longs and lows for shorts while leaving unpaired closes blank.
- Trading/Fusion: harden the 3D Kalman measurement update used by `kalman_physics_error` so malformed OHLC measurements or invalid measurement variance leave the prior state unchanged instead of poisoning later predictions with `NaN`.
- Trading/Optimizer: filter auto-optimizer interval/lookback scopes with the same sweep-threshold split bar requirement enforced by `optimize-equity`, avoiding failed launches on windows that cannot satisfy train/tune/backtest sizing under the configured max-points cap.
- Trading/Optimizer: expose the long-short positioning sampling probability through API optimizer `pLongShort`, the advanced web optimizer form, server env `TRADER_OPTIMIZER_P_LONG_SHORT`, and top-5 wrapper `P_LONG_SHORT`, preserving the existing `--p-long-short` default of `0.2` while making the ROI tradeoff tunable per deployment.
- Trading/Optimizer: expose cost-aware edge sampling probability to server-launched optimizer runs through `TRADER_OPTIMIZER_P_COST_AWARE_EDGE`, matching the existing `--p-cost-aware-edge`, API `pCostAwareEdge`, web form control, and top-5 wrapper `P_COST_AWARE_EDGE`.
- Trading/Optimizer: expose the top-5 wrapper's forced `--bars-auto-prob 0` as `BARS_AUTO_PROB` so fixed-bar audit runs remain the default while research can tune how often optimizer trials use auto-sized history windows.
- Trading/Optimizer: make the shared survivor/prior edge-score formula configurable via `--edge-score-annualized-return-weight`, `--edge-score-sharpe-weight`, `--edge-score-calmar-weight`, `--edge-score-profit-factor-weight`, `--edge-score-walk-forward-sharpe-weight`, `--edge-score-activity-weight`, `--edge-score-calmar-cap`, and `--edge-score-activity-cap`, with matching API `edgeScore*` fields, `TRADER_OPTIMIZER_EDGE_SCORE_*` env forwarding for server-launched optimizer runs, and `EDGE_SCORE_*` CLI forwarding in the top-5 wrapper. Defaults preserve the previous hardcoded edge boost, and prior rows without `createdAtMs` now get a conservative missing-timestamp age multiplier instead of being treated as fresh.
- Deploy/Research: lower Fly and Hetzner research top-combo freshness floor from `0.35` to `0.10`, keeping the 30-day half-life but letting fresh validated candidates pass stale March leaderboard rows more readily.
- Deploy/Research: retune Hetzner optimizer discovery for fresh combo turnover after current cost-floor checks made stale March priors mostly inactive. The research box now uses an `8` trial primary pass plus a `12` trial low-threshold discovery-recovery pass, `180` second trial caps, epochs<=8 / hidden<=40, no exact prior seed, `0.25` stale-prior sampling, threshold candidate sweeps disabled so low-threshold samples do not tune upward into no-trade rows, lower-frequency `2h,4h,6h,12h,1d` scopes, Method 10-heavy weights with some Method 01 exploration, cost-aware entry sampled on `0.35` of trials, inactive ensemble/regime families suppressed, discovery gates of 2 round trips / 1% exposure / zero Sharpe-Calmar floors / walk-forward std<=3, recovery open/close threshold env ranges forwarded through Docker Compose, no Coinbase enrichment by default, and lighter top-combo refresh sweeps (`40` rows every `21600` seconds) so CPU goes to candidate generation.
- Deploy/Hetzner: keep checked-in `*.env.managed` and `*.env.example` overlays in the rsync payload while still excluding secret `*.env` files, so managed research optimizer tuning actually reaches the remote host before the env merge step.
- CI/Docker: split the main workflow's Docker image build into a cache-warming `build-docker` job before Fly deploy, restore Cabal caches with source-aware keys, and restructure the root Dockerfile for dependency-layer caching, BuildKit cache mounts, parallel binary builds, stripped runtime binaries, and a smaller runtime context while preserving public web assets.
- Bot/Live Trading: make the live auto-start transient backoff multiplier ROI-tunable through `TRADER_BOT_AUTOSTART_BACKOFF_MULTIPLIER` (default `2.0`, bounded to `1.0..10.0`) while preserving the existing retry slope by default.
- Bot/Live Trading: expose outcome-weighted LSTM fine-tune coefficients as `TRADER_BOT_OUTCOME_WEIGHT_WIN_SCALE` (default `10`), `TRADER_BOT_OUTCOME_WEIGHT_LOSS_SCALE` (default `25`), and `TRADER_BOT_OUTCOME_WEIGHT_CAP` (default `3`), plus `/bot/start` API fields `botOutcomeWeightWinScale`, `botOutcomeWeightLossScale`, and `botOutcomeWeightCap` and bot-status reporting, so live learning can tune how strongly recent winning and losing trade spans reshape the model.
- Bot/Live Trading: expose live LSTM online fine-tune validation controls as `TRADER_BOT_ONLINE_VAL_RATIO` (default `0`) and `TRADER_BOT_ONLINE_PATIENCE` (default `0`), plus `/bot/start` API fields `botOnlineValRatio` and `botOnlinePatience` and bot-status reporting, preserving the previous no-validation online update by default while allowing ROI sweeps to test early-stopped live adaptation.
- Bot/Live Trading: make the post-operation bot re-optimization training-window cap tunable through `TRADER_BOT_OPTIMIZE_TRAIN_BARS_CAP` (default `1000`) and `/bot/start` field `botOptimizeTrainBarsCap`, with status reporting. The effective window still honors the active lookback minimum while letting deployments test deeper post-trade retuning.
- Trading/Top Combos: make scheduled refresh policy ROI-tunable with `TRADER_TOP_COMBOS_BACKTEST_STALE_DAYS` and `TRADER_TOP_COMBOS_BACKTEST_PRUNE_FINAL_EQUITY_FLOOR`. Defaults preserve the existing behavior: always refresh combos older than 3 days and prune refreshed non-zero-trade performance that does not clear final equity `1.0`.
- Bot/Live Trading: expose live fill-slippage calibration constants as `--cost-calibration-min-observations` (default `8`), `--cost-calibration-shrinkage-obs` (default `16`), `--cost-calibration-window` (default `64`), `--cost-calibration-floor-factor` (default `0.25`), `--cost-calibration-max-per-side` (default `0.01`), and `--cost-calibration-outlier-bound` (default `0.05`), plus matching API camelCase fields and bot-status reporting. Defaults preserve the previous live cost-gate calibration while letting ROI analysis tune how quickly realized fills update slippage assumptions.
- Bot/Live Trading: make live auto-start online-training defaults ROI-tunable through `TRADER_BOT_ONLINE_EPOCHS` (default `3`), `TRADER_BOT_TRAIN_BARS` (default `800`), and `TRADER_BOT_MAX_POINTS` (default `2000`) while preserving `/bot/start` request overrides.
- Trading/Bot: expose Polymarket herd cache timing as `--prediction-market-herd-fresh-ttl-sec` (default `60`) and `--prediction-market-herd-stale-ttl-sec` (default `300`), with matching API camelCase fields, so live-entry herd confirmation can tune freshness versus stale fallback behavior without code changes.
- Trading/Bot: expose Polymarket herd market-selection scoring weights as `--prediction-market-herd-score-base`, `--prediction-market-herd-interval-match-bonus`, `--prediction-market-herd-time-decay-bonus`, `--prediction-market-herd-past-end-penalty`, and `--prediction-market-herd-volume-score-weight`, with matching API camelCase fields. Defaults preserve the previous base/interval/time/expired/volume scoring while allowing ROI sweeps to tune whether herd confirmation prefers interval matches, nearer expiries, or deeper-volume markets.
- Trading/Fusion: expose the `kalman_physics_error` retained sample window, holdout split, volume-adjusted close-bias measurement coefficients, candidate-selection validation split, and optional fixed GBDT correction candidate as `--kalman-physics-bars` (default `1000`), `--kalman-physics-backtest-ratio` (default `0.3`), `--kalman-physics-volume-ewma-alpha` (default `0.1`), `--kalman-physics-volume-signal-clamp` (default `3`), `--kalman-physics-close-bias-scale` (default `0.05`), `--kalman-physics-candidate-validation-ratio` (default `0.2`), `--kalman-physics-small-sample-validation-ratio` (default `1/3`), `--kalman-physics-candidate-trees` (default `0`, built-in grid), and `--kalman-physics-candidate-learning-rate` (default `0`, built-in grid), plus matching `optimize-equity` range flags. Defaults preserve the previous latest-1000/train-700/test-300 and measurement-assimilation/model-selection behavior while letting ROI sweeps and top-combo adoption compare shorter or longer physics-model windows, close-bias coefficients, candidate validation splits, and GBDT correction complexity without code changes.
- Trading/Optimizer: expose the auto optimizer discovery-recovery threshold ranges as `TRADER_OPTIMIZER_DISCOVERY_OPEN_THRESHOLD_MIN/MAX` and `TRADER_OPTIMIZER_DISCOVERY_CLOSE_THRESHOLD_MIN/MAX`, keeping the previous recovery defaults of `0.00005..0.005` for open thresholds and `0.000001..0.005` for close thresholds while making no-eligible retry breadth tunable without code changes.
- Trading/Optimizer: make survivor crossover parent gates ROI-tunable via `--survivor-parent-activity-floor` and `--survivor-parent-annualized-return-floor`, plus API camelCase fields, web advanced-run controls, server env overrides `TRADER_OPTIMIZER_SURVIVOR_PARENT_*`, and top-5 wrapper `SURVIVOR_PARENT_*` env settings. Defaults preserve the previous hardcoded survivor-crossover eligibility filter of `opCount > 5` and `annualizedReturn > 1`.
- Trading/Optimizer: add `--survivor-edge-weight` plus API `survivorEdgeWeight`, web advanced-run control, top-5 wrapper `SURVIVOR_EDGE_WEIGHT`, and server env `TRADER_OPTIMIZER_SURVIVOR_EDGE_WEIGHT`, so exploitation refreshes rank survivor neighborhoods by optimizer score plus a tunable positive-edge boost from return, Sharpe, Calmar, profit factor, walk-forward Sharpe, and activity evidence.
- Trading/Optimizer: add `--survivor-rank-bias` plus API `survivorRankBias`, web advanced-run control, top-5 wrapper `SURVIVOR_RANK_BIAS`, and server env `TRADER_OPTIMIZER_SURVIVOR_RANK_BIAS`, so survivor perturbation/crossover draws spend more budget on top edge-adjusted survivors while still sampling the eligible tail. Set it to `0` for uniform/round-robin survivor exploitation.
- Trading/Optimizer: add default-on server env `TRADER_OPTIMIZER_AUTO_CAPPED_LOOKBACKS`, deriving the largest split-feasible lookback for any configured interval whose `TRADER_OPTIMIZER_LOOKBACK_WINDOWS` would exceed `TRADER_OPTIMIZER_MAX_POINTS`. Research nodes with tight fetch caps can now keep `5m`/`15m` discovery in the auto optimizer pool instead of silently excluding those scopes.
- Trading/Optimizer: apply the auto optimizer discovery threshold/hold/min-edge/SNR ranges to the primary pass as well as the recovery pass, while keeping primary deployability gates intact. The first research trials now search the low-threshold activity-producing space immediately instead of spending the primary budget on broad/default threshold samples before retrying.
- Trading/Optimizer: make router and bandit-router same-regime evidence floors ROI-tunable via `--router-regime-min-bars`, `--router-regime-min-fraction`, API camelCase params, `optimize-equity` range flags, top-combo JSON persistence, correlation-guided optimizer guesses, web controls, and top-5 wrapper `ROUTER_REGIME_*` env ranges while preserving the previous default effective floor of `max 3 (floor (lookback * 0.25))`.
- Trading/Optimizer: add `--prior-edge-weight` plus top-5 wrapper `PRIOR_EDGE_WEIGHT` and server `TRADER_OPTIMIZER_PRIOR_EDGE_WEIGHT`, so empirical-prior sampling ranks prior top-combo neighborhoods by the active objective plus a tunable positive-edge boost from annualized return, Sharpe, Calmar, profit factor, walk-forward Sharpe, and activity evidence. Prior selection stderr now includes selected-pool edge average/max telemetry.
- Trading/Optimizer: use top-combo `processing.rankScore` as a rank floor when importing empirical priors, so freshness-aware deployable rows keep their sampling influence when they become prior neighborhoods for the next optimizer run.
- Trading/Optimizer: expose `--prior-missing-age-multiplier`, top-5 wrapper `PRIOR_MISSING_AGE_MULTIPLIER`, and server `TRADER_OPTIMIZER_PRIOR_MISSING_AGE_MULTIPLIER` so prior rows missing `createdAtMs` can be discounted more aggressively during freshness-biased searches. Research deploys use `0.10`; the CLI default remains `0.25`.
- Trading/Optimizer: expose hardcoded technical-analysis, predictor-training, volatility-confidence gate, adaptive performance-gate, ensemble-blend, fresh-entry signal-gate, tri-layer price-action shape, and ROI objective-scoring constants as ROI-optimizable parameters. Backtests/latest-signal runs can tune TA entry threshold, ADX/RSI/Stochastic/MFI/envelope/regime-switch thresholds, ATR stop/take-profit multipliers, `ta_best` confidence, TA confidence-scoring coefficients, predictor GBDT/conformal/TCN ridge/quantile/KNN/decision-tree/transformer/HMM training calibration constants, volatility/confidence bucket thresholds and size multipliers, adaptive win-rate/profit-factor ramp slack, blend softmax/net-softmax scales, edge-blend power, smooth-softmax alpha, hedge-blend learning/error caps, divergence shrinkage, regime-switch/router/performance-regime cutoffs, regime-selector ADX/Aroon/slope/width scoring floors/spans/weights, bandit-router exploration, anchor-blend neutral-anchor coefficients, coherence-gate shrink/boost coefficients, entropy-blend alpha coefficients, fractal-blend return/gain coefficients, phase-cancel return/conflict/alignment coefficients, tension-gate shrink coefficients, signal headroom/spike/trend-slack/directionality/predictor-liveness coefficients, tri-layer hammer/shooting-star wick/body plus railroad-track body-tolerance ratios, the tri-layer default body/open-threshold multiplier (`--tri-layer-price-action-body-open-threshold-mult`, default `0.25`), and ROI score expectancy/payback/activity/exposure evidence penalties through new CLI flags; optimizer/tune simulations now consume those blend, regime, and tri-layer coefficients through `EnsembleConfig`, and `optimize-equity` samples the same values with matching `*-min` / `*-max` ranges and emits them into trial/top-combo JSON for parameter-to-ROI analysis. Top-combo adoption now hydrates those stored TA/predictor/adaptive/vol-conf/blend/regime/signal/tri-layer params back into runtime args, and cached/latest-bar breakout paths share the calibrated breakout take-profit multiplier.
- Trading/Calibration: expose threshold-calibration headroom, fee-floor, sample-size, and conservative/aggressive percentile constants through `ThresholdCalibrationConfig` plus `calibrateThresholdWithConfig`, preserving existing defaults while letting ROI analysis tune the report thresholds instead of recompiling.
- Trading/Optimizer: add empirical prior sampling to `optimize-equity` via `--prior-json`, `--prior-sample-prob`, `--prior-method-sample-prob`, `--prior-rank-bias`, `--prior-top-fraction`, `--prior-min-samples`, `--prior-perturb-scale-double`, `--prior-perturb-scale-int`, `--prior-age-half-life-days`, `--prior-diversity-max-per-bucket`, and `--prior-seed-count`, with the top-5 wrapper forwarding `PRIOR_*` env settings and server-launched optimizer runs forwarding `TRADER_OPTIMIZER_PRIOR_*` settings against the current top-combo file so new searches can spend budget near previously validated top-combo neighborhoods before broad random exploration. Prior rows are ranked after evidence filtering with the active optimizer objective and ROI penalty config, falling back to their stored leaderboard score only when metrics are unavailable; prior-guided samples can also adopt enabled method families from validated prior rows so strong historical methods get budget even when the base random method mix would under-sample them, rank-biased prior draws spend more budget on the strongest eligible rows without removing tail exploration, dedicated prior perturb scales keep empirical-prior jitter tighter than broader survivor mutations by default, age-half-life scoring discounts older prior rows so stale winners do not keep dominating new searches, the per platform/interval/method diversity cap keeps one stale bucket from monopolizing the prior pool, deterministic prior seeding evaluates the strongest selected priors at the start of the seed phase, and the exploitation-phase survivor pool now refreshes with newly eligible high-score trials so later perturbation/crossover budget follows post-seed discoveries. Legacy prior rows that lack `metrics.walkForwardSummary` remain usable as sampling hints while fresh trials still have to pass active walk-forward gates before ranking/deployment; optimizer stderr now reports selected prior method/interval counts plus walk-forward and timestamp coverage, and optimizer record/top-combo JSON emits `optimizerTrialOrigin` so edge can be compared by seed, prior, and survivor source.
- Deploy/Research: cap Hetzner auto-optimizer fetch depth at `TRADER_OPTIMIZER_MAX_POINTS=1000`, keeping background discovery closer to the current high-ROI bar-count neighborhood and avoiding slow 5k-bar first trials when the goal is fresh combo turnover.
- Trading/Predictors: expose LSTM Adam coefficients as `--lstm-adam-beta1`, `--lstm-adam-beta2`, and `--lstm-adam-eps` for `trader-hs` and `optimize-equity`, plus `optimize-equity` sampling ranges `--lstm-adam-beta1-min/max`, `--lstm-adam-beta2-min/max`, and `--lstm-adam-eps-min/max`; API optimizer range fields `lstmAdamBeta1Min/Max`, `lstmAdamBeta2Min/Max`, and `lstmAdamEpsMin/Max`; top-5 wrapper `LSTM_ADAM_*` fixed/range env controls; and server-launched optimizer fixed/range `TRADER_OPTIMIZER_LSTM_ADAM_*` env controls. Optimizer records/top combos now persist sampled Adam values, and top-combo adoption hydrates them back into runtime args, preserving previous defaults while allowing optimizer/research runs to tune training stability without code changes.
- Trading/Optimizer: expose the threshold sweep downsampling cap as `--tune-max-threshold-candidates` (default `60`) for `trader-hs` and `optimize-equity`, plus API `tuneMaxThresholdCandidates` and `TRADER_OPTIMIZER_TUNE_MAX_THRESHOLD_CANDIDATES` for server-launched runs, so ROI searches can tune threshold granularity versus runtime without code changes.
- Trading/Fusion: expose sensor residual variance EWMA alpha as `--sensor-variance-ewma-alpha` (default `0.05`), API `sensorVarianceEwmaAlpha`, and `TRADER_OPTIMIZER_SENSOR_VARIANCE_EWMA_ALPHA` for optimizer-launched runs, making Kalman sensor weighting adaptation ROI-tunable instead of fixed in code. The learned residual variance now floors model-reported sigma before fusion. Conservative Kalman fusion can inflate correlated sensor measurements and large innovations through `--kalman-sensor-correlation-inflation`, `--kalman-innovation-inflation-threshold`, and `--kalman-innovation-inflation-max`, matching API camelCase fields, and `TRADER_OPTIMIZER_KALMAN_*` env controls for optimizer-launched discovery. Optimizer records and top-combo payloads now persist those fusion settings so prior sampling, correlation analysis, and top-combo adoption can round-trip them. `kalman_physics_error` training now uses the same point-in-time `v*v` feature as inference, removing the next-close feature leak.
- Trading/Optimizer: expose the server-side auto optimizer's hardcoded method-family sampling weights as `TRADER_OPTIMIZER_METHOD_WEIGHT_*` environment knobs. Defaults preserve the current LSTM-heavy discovery mix, and the resolved weights still flow through the existing live-gap multiplier before being passed to `optimize-equity`.
- Trading/Optimizer: expose the auto optimizer's live-gap evidence floors and sampling multiplier clamps as `TRADER_OPTIMIZER_LIVE_GAP_MIN_COMBO_OPERATIONS`, `TRADER_OPTIMIZER_LIVE_GAP_MIN_TOTAL_OPERATIONS`, `TRADER_OPTIMIZER_LIVE_GAP_MULTIPLIER_FLOOR`, and `TRADER_OPTIMIZER_LIVE_GAP_MULTIPLIER_CEILING`, keeping the previous defaults while making the live-vs-backtest feedback loop ROI-tunable at deploy time.
- Trading/Top Combos: scheduled top-combo backtests now include every combo whose backtest freshness is older than 3 days, even when it sits outside `TRADER_TOP_COMBOS_BACKTEST_TOP_N`. Refreshed combos with sub-1.0 performance are pruned via the normal update path, and the payload carries drop tombstones so stale DB/S3/state-sync replicas cannot resurrect the old score. Zero-trade refresh windows still stamp freshness without overwriting optimizer metrics.
- Trading/Top Combos: add opt-in freshness-aware merge ranking through `--score-freshness-half-life-days` (default `0`, disabled) and `--score-freshness-floor-multiplier` (default `0.35`), plus matching server env overrides `TRADER_TOP_COMBO_SCORE_FRESHNESS_HALF_LIFE_DAYS` and `TRADER_TOP_COMBO_SCORE_FRESHNESS_FLOOR_MULTIPLIER`. When enabled, positive `processing.validatedScore` is age-adjusted from `createdAtMs` into a new `processing.rankScore`, so fresh deployable candidates can outrank stale historical rows without relaxing trade-count, walk-forward, or live-quarantine gates. Fly and Hetzner research enable a `30` day half-life with a `0.10` floor for fresh combo turnover.
- Trading/Top Combos: top-combo ranking now applies the same trade-count evidence floor used for live adoption, so legacy one- or two-trade annualized-return outliers sink below deployable candidates instead of occupying the first leaderboard slots.
- Trading/Top Combos: merge processing now collapses source/null-equivalent duplicate strategy rows before ranking and annotates each combo with `processing.tier`, `processing.validatedScore`, and evidence-gap reasons. Ranking prefers `deployable` walk-forward-validated combos over unvalidated `candidate` rows, then falls back to raw/quarantined rows.
- Trading/Top Combos: move live-blend, live-quarantine, annualized-return clamp, walk-forward multiplier, drawdown penalty, and equity-term scoring constants into `Trader.TopComboScoring`. `merge-top-combos` now exposes them through `--score-*` flags, and server-side optimizer merges forward matching `TRADER_TOP_COMBO_SCORE_*` env overrides so top-combo ranking coefficients can be optimized for ROI without rebuilding.
- Trading/Optimizer: optimizer JSONL records and generated top-combo payloads now persist `backtest.walkForward.summary` under `metrics.walkForwardSummary`, so combo processing can promote fresh trials to the `deployable` tier when they clear the walk-forward Sharpe evidence floor.
- Bot/Live Trading: expose the live top-combo adoption position-size cap as `--adoption-max-position-size-cap` (default `0.25`) so deployments can tune the legacy-combo exposure ceiling without changing optimizer samples.
- Bot/Live Trading: expose live top-combo adoption evidence gates as `--adoption-min-trade-count` (default `20`) and `--adoption-min-wf-sharpe-mean` (default `0.3`), with matching API params and status/cache JSON fields, so deployments can optimize the trade-count and walk-forward Sharpe floors that decide whether stored combos deserve live capital.
- Bot/Live Trading: keep `TRADER_BOT_AUTOSTART=false` parked inside the auto-start worker instead of returning immediately and being restarted by the supervisor, so deployments can disable default bot autostart without flooding logs or reintroducing fallback bot starts.
- Web/Ops: add Open Instance and Close Instance attribution to Binance account trades and CSV exports, derived from the persisted server identity of matched `trade.order` / `bot.order` rows. New ops rows now persist `server_id`, `server_role`, and `server_provider`, `/ops` responses expose those fields, and Binance trade payloads include `originInstance` so the UI can display `laptop`, `hetzner`, or `fly` separately from the existing IP diagnostics.
- Trading/Optimizer: make the server-side auto optimizer sample feasible `(interval, lookbackWindow)` scopes via new `TRADER_OPTIMIZER_LOOKBACK_WINDOWS`, so research nodes can search `5m`/`15m` windows without forcing the whole optimizer onto a short or long global lookback. Fly and Hetzner research now include `5m,15m,1h,2h,4h,6h,12h,1d` with `7d,14d,30d` windows, `TRADER_OPTIMIZER_MAX_POINTS=5000`, and `TRADER_OPTIMIZER_MAX_COMBOS=1000` so fresh candidates can surface below the stale top-500 rank wall while refresh sweeps deflate old scores; Hetzner research also defaults to `TRADER_OPTIMIZER_PRIOR_SAMPLE_PROB=0.75`, `TRADER_OPTIMIZER_PRIOR_METHOD_SAMPLE_PROB=0.8`, and `TRADER_OPTIMIZER_PRIOR_RANK_BIAS=2.5` to spend more background budget near validated correlation/leaderboard neighborhoods. Discovery recovery now also treats cost-floor, Sharpe/Calmar, and walk-forward filter reasons as retryable zero-eligible runs instead of only retrying activity/exposure/open-threshold/timeouts, so strict production gates do not leave the combo board frozen after the primary pass rejects every trial. Optimizer JSONL records now include `createdAtMs`, so auto/manual merges produce fresh combo timestamps instead of adding null-stamped rows that newest-first UI views can miss.
- Trading/Adoption: mirror the optimizer's two production gates at top-combo adoption time so legacy leaderboard combos cannot leak into live trading if the cost-floor filter is ever relaxed. New `Trader.BotStartSemantics` exports `adoptionMinTradeCount = 20` (matches `TRADER_OPTIMIZER_MIN_ROUND_TRIPS` production gate) and `adoptionMinWalkForwardSharpeMean = 0.3` (matches `runAutoOptimizerLoop`'s `minWfSharpeMean` default), plus pure predicates `comboTradeCountMeetsAdoptionFloor` and `comboWalkForwardSharpeMeetsAdoptionFloor`. New `Main.hs` helpers `topComboTradeCountBelowFloor` and `topComboWalkForwardSharpeBelowFloor` read `metrics.tradeCount` and `metrics.walkForwardSummary.sharpeMean` directly from the combo's stored metrics; both are wired into `selectCompatibleTopComboArgs` (per-symbol adoption) and `topCombosTopTargets` (fleet adoption) alongside the existing `topComboMinEdgeBelowCostFloor` and `topComboLiveQuarantined` filters. Closes the 2026-06-14 leaderboard pathology where 500/500 prod top combos had median `tradeCount=4` (sampling-variance-dominated Sharpe estimates of 8.5) and 0/500 carried any `walkForwardSummary`; the cost-floor caught all 500 today, but a future cost-floor relaxation would otherwise let the same low-evidence combos through. Adds six new test invariants pinning the thresholds to the optimizer-side defaults (`testAdoptionMinTradeCountMatchesOptimizerProductionGate`, `testComboTradeCountMeetsAdoptionFloorMonotonicity`, `testComboTradeCountMeetsAdoptionFloorMatchesProductionRegressionEvidence`, `testAdoptionMinWalkForwardSharpeMatchesOptimizerDefault`, `testComboWalkForwardSharpeMeetsAdoptionFloorFailsClosed`, `testComboWalkForwardSharpeMeetsAdoptionFloorMonotonicity`), so future drift between the two gates is detected at test time. See `ENGINEERING_REVIEW_2026-06-14.md`.
- Deploy/Hetzner: keep research on the same tenant as trading by deriving `TRADER_STATE_SYNC_TENANT_KEY` from the shared Binance key pair instead of the old `trader-prod` placeholder. CI deploys can now merge a temporary secret env override into the remote Hetzner env file, and the Hetzner workflow applies `HETZNER_BINANCE_API_KEY` / `HETZNER_BINANCE_API_SECRET` (falling back to the trading-named secrets) to both trading and research so `/binance/keys`, state sync, and live-bot state use one derived `binance:<hash>` tenant.
- Deploy/CI: retry Hetzner SSH deploys with bounded connect timeouts and keep both trading and research deploys mandatory, so each role must advance to the latest green commit. Manual Hetzner dispatch now redeploys the latest successful CI commit for the selected branch instead of the branch tip. The Hetzner rsync path now excludes local Cabal/Stack/Python/temp caches (`.cabal`, `.stack-root`, `.stack-work`, `.venv`, `.tmp`) to avoid multi-GB deploy syncs.
- Web/Positions: add a top-of-section digest for open Binance futures positions, summarizing total open PnL, PnL/notional, long/short exposure, estimated margin, weighted leverage, nearest liquidation distance, and largest open position before the per-position charts.
- Trading/Risk: cap the live-adoption `maxPositionSize` at 0.25 and tighten optimizer sampling to `[0.15, 0.25]` (was `[0.6, 0.9]`). New `Trader.BotStartSemantics` helpers `adoptionMaxPositionSizeCap = 0.25` and `capAdoptedMaxPositionSize` clamp any legacy combo's stored `maxPositionSize` when `applyTopComboForStart` adopts it for live trading; the optimizer's `OptimizeEquityMain.hs` `--max-position-size-min/max` defaults shift from `[0.6, 0.9]` to `[0.15, 0.25]` so newly sampled combos stay inside the same envelope. Closes the 2026-06-13 cliff shape where adopted combos carried `maxPositionSize` up to ~0.78 (median) at 10–20x perp leverage, turning every 2% adverse move into a 16–40% account hit. Adds `testCapAdoptedMaxPositionSizeBoundsLiveExposure` invariant covering input saturation, negative collapse, and non-finite handling.
- Trading/Optimizer: turn walk-forward Sharpe stability gates on by default in the auto optimizer. `minWfSharpeMean` defaults from 0 (off) to 0.3 and `maxWfSharpeStd` defaults from 0 (off) to 1.5 in `runAutoOptimizerLoop`, so trials with positive ROI on a single fold but negative or wildly variable Sharpe across folds no longer enter the leaderboard. Still overrideable via `TRADER_OPTIMIZER_MIN_WF_SHARPE_MEAN` / `TRADER_OPTIMIZER_MAX_WF_SHARPE_STD` (set to 0 to restore the prior behavior).
- Trading/Costs: enforce a venue cost floor on `minEdge` everywhere it is sampled or read. `Trader.CostCalibration` now exports `venueRoundTripCostFloor = 2*(fee+slippage)+spread = 1.2e-3` and `venueMinEdgeFloor = 1.5 * venueRoundTripCostFloor = 1.8e-3`. The optimizer sampler clamps `minEdge = max venueMinEdgeFloor sample` (`Trader.Optimizer.Optimize`), the trial deployability gate rejects any trial whose `tpMinEdge < venueMinEdgeFloor` with reason `minEdge<…(costFloor)`, and combo selection (`selectCompatibleTopComboArgs`, `topCombosTopTargets`) refuses to adopt any top combo whose stored `minEdge` is below the floor via the new `topComboMinEdgeBelowCostFloor`. The auto-loop's hard-coded discovery-recovery range (previously `--min-edge-min 0 --min-edge-max 0.0005`, i.e. 0–5 bp) is now lifted to `[venueMinEdgeFloor, max(3*venueMinEdgeFloor, 60bp)]`, and the `OptimizeEquityMain` default range moves from `[2 bp, 10 bp]` to `[20 bp, 60 bp]`. Closes the 2026-06-13 prod regression where every one of 500 top combos sat below the round-trip cost (median sampled `minEdge` 4.8 bp vs median sampled round-trip cost 23 bp), guaranteeing the live system would leak edge to the venue regardless of how good the backtest looked. Adds three new test invariants (`testVenueRoundTripCostFloorMatchesVenueCosts`, `testVenueMinEdgeFloorClearsRoundTripCost`, `testVenueMinEdgeFloorMatchesProductionRegressionEvidence`).
- CI/Autoloop: after a green CI push to `main`, refresh the locally running stack so the API and Web UI match the just-pushed HEAD. New `scripts/restart-local-stack.sh` rewrites `haskell/.build-commit` to `git rev-parse HEAD` (the SHA `/health` reports) and `launchctl kickstart -k`s `ai.openclaw.trader.api` and `ai.openclaw.trader.web`; the autoloop calls it from the green-CI branch and records the outcome under `localRefresh` in the status payload. Best-effort and macOS-only: skipped automatically off-Darwin or when `launchctl`/services are missing, and opt-outable with `AUTOLOOP_SKIP_LOCAL_REFRESH=1`. Closes the gap where the running API kept reporting an old commit (e.g. `b5a5fc73`) after many merges to `main` (`d84cc09…`), which delayed the deployment of the pooled-family quarantine fix (`3a92ad9f`) and let symbols like ETCUSDT keep losing money on an out-of-date binary.
- Bot/Live Trading: add a default capital-preservation gate for live Binance futures bots. New exposure is held when current drawdown reaches 10%, the bot hits three consecutive closed-trade losses, the last 20 closed trades lose at least 5%, or rolling trade Sharpe turns negative after the minimum sample; reversal attempts reduce to flat instead of flipping, and `/bot/status` now exposes `performance.capitalPreservation`.
- Bot/Live Trading: expose the capital-preservation coefficients as CLI/API parameters: `capitalPreservationLookback`, `capitalPreservationMinTrades`, `capitalPreservationMaxDrawdown`, `capitalPreservationMaxRollingLoss`, `capitalPreservationMinSharpe`, and `capitalPreservationLossStreakMax` (CLI `--capital-preservation-*`). Defaults preserve the current 20-trade lookback, 6-trade readiness floor, 10% drawdown, 5% rolling loss, zero Sharpe, and 3-loss streak gates while letting live deployments tune the ROI/risk tradeoff without code changes.
- Trading/Predictors: make KNN prediction sanitize malformed fallback sigma values, so corrupted model state cannot return `NaN`/`Infinity` uncertainty when malformed queries or model rows fail closed.
- Trading/Predictors: make DecisionTree prediction drop malformed sigma estimates and fail closed on non-finite leaf values or split thresholds, preventing malformed tree state from surfacing `NaN`/`Infinity` sensor output.
- Trading/Predictors: make HMM regime fitting/filtering/update paths ignore malformed observations and normalize malformed model probabilities safely, keeping regime probabilities, expected returns, and sigma finite under corrupted evidence.
- Trading/Predictors: make TCN feature extraction, training, and prediction reject non-finite price windows, targets, weights, and outputs so malformed series data cannot produce `NaN`/`Infinity` sensor forecasts.
- Trading/Predictors: make quantile training discard non-finite or dimension-inconsistent rows and reject non-finite learning-rate/regularization inputs, so corrupted calibration evidence cannot produce malformed quantile weights.
- Trading/Predictors: make GBDT training discard non-finite or dimension-inconsistent rows and make prediction fail closed on malformed query/model values, preventing corrupted feature evidence from emitting `NaN`/`Infinity` forecasts.
- Trading/Prediction Markets: round exchange intervals up to the next supported Polymarket horizon for herd confirmation instead of choosing the absolute nearest interval, so 30m/2h/12h signals are not confirmed by shorter 15m/1h/4h markets.
- Trading/Top Combos: make stamped backtest refreshes beat stale duplicate combos with missing `createdAtMs`, so legacy payloads cannot resurrect an older higher score when no comparable freshness timestamp exists.
- Trading/Coinbase: make cross-exchange Coinbase close alignment fail open when the Binance open-time grid and close vector lengths disagree, instead of emitting a mismatched feature vector with synthetic zero fallback prices.
- Trading/Calibration: make arbitrary `PercentileMethod` threshold requests interpolate between stored distribution percentiles instead of snapping upward to the next bucket, so values like P80 and P1 behave continuously and do not overstate calibrated entry thresholds.
- Bot/Live Trading: harden the top-combo startup backtest guard so it (a) does not abort live starts on under-sampled smoke windows and (b) never deletes a combo on a guard verdict. (a) `botStartupBacktestVerdictWithMinTrades` requires `tradeCount` to reach a configurable minimum (default 3, override via `TRADER_BOT_START_BACKTEST_MIN_TRADES`) before a sub-threshold `finalEquity` becomes `BacktestAbort`; below the threshold the verdict is `BacktestNoVerdict` and the start is allowed. (b) the guard now persists smoke metrics via the keep-all variant (`applyComboUpdatesKeepAllWithStats` / new `applyStartupComboBacktestUpdateKeepAll`) and `botStartupGuardShouldPrune` is `False` for every verdict, so the bot-start path no longer deletes the combo from the top-combos JSON store or the `combos` DB row — pruning is the optimizer / periodic-refresh's job. Today's launchd log (2026-06-12) showed the same combo UUIDs being pruned 2–3 times inside one process (the fingerprint of a prune/S3-resurrect race) and AAVEUSDT smoke windows aborting starts on a single ~5% drawdown (one daily ATR) for combos with out-of-sample equity ≥ 1.42. Adds three new test invariants (under-min-trades guard, default-minTrades pin, never-prune contract). See `ENGINEERING_REVIEW_2026-06-12.md`.
- CI/Autoloop: stop auto-resolving merge conflicts in the forever runner's branch reconciliation. The old policy (`git restore --source=HEAD` on conflicted paths, i.e. keep the base side of conflicted files while cleanly-merged files keep the branch side) produced semantically torn trees that broke `main` twice (the automated merges behind #147 and #150) and silently dropped tested branch work. `mergeRefOntoBaseBranch` now aborts conflicted merges (`git merge --abort`), reports `conflict-aborted` with the conflicted paths, and leaves the branch unmerged and logged for operator review. Reconciliation halts with `ok: false` when syncing the base branch with origin conflict-aborts — including after a rejected push retry, where the guard runs before merged-ref pruning so refs for unpushed merges are never deleted. Operator action replaces the old `autoloop: resolve conflicts while merging …` commits.
- CI/Autoloop: add a build gate to the autoloop branch-reconciliation merge path so a non-building tree can no longer reach `origin/main`. After merging unmerged branches into the base branch, `reconcileUnmergedBranchesOntoBaseBranch` now compiles the reconciled tree (`cabal build all`, ghcup-bootstrapped like the verify commands) before pushing; on failure it hard-resets the whole batch back to the origin-synced head and skips the push, leaving the offending branches unmerged for the next cycle / human review. Toggle with `AUTOLOOP_MERGE_BUILD_GATE=0`. This closes the failure mode where the `feat/research-harness` conflict resolution took `main`'s side of `Predictors/Features.hs` (dropping the Coinbase block) while keeping `Main.hs`'s callers, landing a `main` that did not compile. Also reorder `scripts/verify.sh haskell` to run `cabal build` before fourmolu/hlint so a compile break surfaces on its own instead of being masked by a lint failure under `set -e`.
- Trading/Optimizer: make the server-side auto optimizer's LSTM sampling caps configurable via `TRADER_OPTIMIZER_EPOCHS_MAX` and `TRADER_OPTIMIZER_HIDDEN_SIZE_MAX` (defaults keep the previous hardcoded `epochs<=2` / `hidden<=16`), applied to both the primary and discovery-recovery passes; raise the research-node budgets to fewer, heavier trials so discovery can reach the region where the incumbent top combos live (e.g. #77 LINKUSDT: epochs 8, hidden 29) instead of stalling on tiny-LSTM candidates that cannot beat the leaderboard. Fly research (`fly.research.toml`) gets `epochs<=12` / `hidden<=48`, trials 60→18, per-trial timeout 120s→480s; Hetzner research becomes the bigger producer at `epochs<=16` / `hidden<=64`, 24 trials, 600s timeouts, 2000 points.
- Deploy/CI: deploy the `trader-hs-research` Fly app from CI alongside the backend (`FLY_API_TOKEN_RESEARCH` / `FLY_RESEARCH_APP` secrets optional, falling back to the primary token and `trader-hs-research`), and add a checked-in managed env overlay (`deploy/hetzner/trader.<role>.env.managed`) that the Hetzner deploy merges update-or-append into the box's secret env file before `compose up`, so non-secret optimizer tuning ships from the repo instead of by hand.
- Bot/Live Trading: make the auto-start backoff log-dedup and `sbLastErrorMessage` fingerprint stable across rotating Binance auth-reply noise (the embedded `request ip`, `signature`, `timestamp`, `recvWindow`, `serverTime`, `listenKey`, `x-mbx-uuid`, and `request-id` tokens), so a host whose egress IP rotates during a Binance `-2015` outage no longer re-logs the same failure once per rotation and the in-memory backoff record's "stable fingerprint" claim is actually true. Adds a new pure `normalizeAutoStartErrorMessage` in `Trader.App.AutoStartBackoff` (idempotent, classification-preserving), wires it into `recordError` for log dedup and into `initialBackoff`/`nextBackoff` for the backoff fingerprint, and extends `Trader.Test.AutoStartBackoff` with seven new invariants (IP-rotation stability, signature/timestamp/listenKey rotation stability, idempotency, classification preservation, backoff-record stability across IP rotation). Closes the 2026-06-10 launchd-log regression where the same `-2015` failure was re-logged once per `request ip:` value (5 IPs/symbol observed in one day).
- Bot/Live Trading: add an auto-start circuit breaker and per-symbol exponential backoff so the live-bot auto-start loop no longer hammers Binance every 30s during permanent auth failures (HTTP 401 / Binance code `-2015`, `-2014`, `-1022`, `-1021`) or DNS/5xx outages. A new `Trader.App.AutoStartBackoff` module classifies each failure as `ErrAuth` / `ErrTransient` / `ErrPermanent` / `ErrUnknown`; auth failures and Binance-validation rejections open a long per-symbol circuit, transient errors grow geometrically up to `TRADER_BOT_AUTOSTART_BACKOFF_MAX_SEC`, and the loop pauses globally when at least `TRADER_BOT_AUTOSTART_AUTH_CIRCUIT_THRESHOLD` symbols are auth-failed. Tunable via `TRADER_BOT_AUTOSTART_BACKOFF_INITIAL_SEC`, `TRADER_BOT_AUTOSTART_BACKOFF_MAX_SEC`, `TRADER_BOT_AUTOSTART_AUTH_OPEN_SEC`, `TRADER_BOT_AUTOSTART_PERMANENT_OPEN_SEC`, `TRADER_BOT_AUTOSTART_AUTH_CIRCUIT_THRESHOLD`, and `TRADER_BOT_AUTOSTART_AUTH_CIRCUIT_OPEN_SEC`. Adds the previously-unwired `Trader.Test.BinanceProbe` suite and a new `Trader.Test.AutoStartBackoff` suite covering classification, monotone backoff, cap clamping, and circuit-open thresholds.
- Web: automatically fit an over-large lookback window/override down to available capped bars when the UI cannot fix the mismatch by increasing `bars`, so valid exchange/API caps no longer leave request buttons disabled behind a manual Lookback fix.
- Ops/Deployment: label every persisted operation and journal event with `serverId`, `serverRole`, and `serverProvider`; persist the server that opened live-bot positions so close/reduce order rows can report opener and closer server attribution.
- Deploy: add a Hetzner Docker Compose production deployment with split trading and research server roles, local Postgres, Caddy TLS/path isolation, browser Basic Auth with same-origin API-token injection, systemd restart/watchdog timers, state sync, and daily backup hooks as the cost-effective always-on path for the stateful API.
- Trading/CLI: reject `--max-position-size` values `<= 0` instead of allowing zero; the error message now reads `must be > 0` instead of `must be >= 0`, matching the existing `--initial-balance` and `--trailing-stop` strict-positive contracts.
- Trading/TA: add `ta_regime_switch`, a regime-conditioned technical-analysis method that uses ADX/EMA200 to select long-biased trend following, narrow Bollinger bandwidth to select reversion, and breakout otherwise.
- Planning: add next-generation architecture RFC (`artifacts/planning/trader-firm-nextgen-architecture-2026-05-20.md`) synthesizing 12 transformative trading-system ideas into a phased implementation plan.
- Web/Trade: default new trade forms to live armed trading and quote-fraction sizing with `orderQuoteFraction=1`, `orderQuote=0`, and `orderQuantity=0`, matching the Live Bot & Trade preset.
- Deploy: keep research producers focused on optimizer discovery by disabling bot autostart and scheduled top-combo rechecks in the checked-in Fly/Hetzner research defaults, with smaller per-run optimizer budgets so combo refreshes can continue without starving the API.
- Trading/Optimizer: normalize generated min/max position sizes before launching each optimizer trial and stop sampling disabled method `11`, preventing generated trials from failing before they can be scored.
- Trading/Optimizer: make the server-side auto optimizer use a method-01-heavy (LSTM) discovery mix with conformal/quantile confirmations disabled by default. The prior method-10-heavy (Kalman) mix produced near-zero deployable candidates: across ~4,800 background trials only 4 were ever eligible because method 10 (Kalman-only) emits no directional signal on the daily series (every trial stalls at `KALMAN_NEUTRAL`, `activity=0/1`, finalEquity pinned at 1.0), while method 11 is disabled (LSTM init hang) and `blend` stays inert. Direct backtests confirm method 01 (LSTM) is the only path that opens/closes positions and is profitable untuned on BTC/SOL/BNB, so discovery now concentrates its search there to keep the live combo pipeline (and hands-off bot autostart) supplied with tradeable candidates.
- Web/Optimizer: add parameter-to-ROI correlation charts to the Optimizer Combos section, scoped to the active combo filters, plus correlation-guided optimizer guesses that prefill run ranges and advanced extra JSON knobs from the strongest ROI relationships.
- Web: show loaded open-position and orphaned-position counts next to the Positions and Orphaned ops items in the Sections menu.
- Web/Live Bot: order Live Bot symbol selectors with symbols that have currently open Binance futures positions before flat symbols, then keep the existing active/running/startup ordering.
- Trading/Optimizer: make the server-side auto optimizer pass explicit deployability gates and expose `TRADER_OPTIMIZER_MIN_ROUND_TRIPS`, `TRADER_OPTIMIZER_MIN_EXPOSURE`, `TRADER_OPTIMIZER_MIN_SHARPE`, `TRADER_OPTIMIZER_MIN_CALMAR`, `TRADER_OPTIMIZER_MIN_WF_SHARPE_MEAN`, and `TRADER_OPTIMIZER_MAX_WF_SHARPE_STD`; the auto loop now defaults to discovery-friendly gates instead of inheriting the `optimize-equity` CLI's 20-round-trip production filter.
- Trading/Optimizer: add a server-side auto optimizer discovery-recovery pass for no-eligible runs dominated by inactive, timeout, threshold, or exposure filters; the retry uses lower discovery gates, lower threshold ranges, shorter hold/cooldown ranges, and active method weights, exposed through `TRADER_OPTIMIZER_DISCOVERY_*` settings.
- Bot/Live Trading: prioritize symbols with orphaned futures positions ahead of requested, env, and top-combo bot-start targets before immediate-start limits, queues, and auto-start max-bot caps are applied.
- Bot/Live Trading: run a fresh Binance-backed backtest before starting any bot tied to a top-combo UUID; if the refreshed combo no longer has positive ROI, prune it from top combos and abort that startup.
- Trading/Bot: add opt-in `--prediction-market-herd` live entry confirmation using Polymarket crypto up/down markets for the corresponding symbol and nearest supported interval; same-side odds can boost confidence/size, while opposing or below-floor odds hold fresh entries as `PREDICTION_MARKET_HERD`.
- Binance/API: migrate Spot user-data stream startup from removed REST listenKey endpoints to Binance's WebSocket API `userDataStream.subscribe.signature`; Spot keep-alive and close actions now operate on the backend WebSocket session, while futures and margin keep using REST listen keys.
- Ops/API: add an idempotent migration for existing PostgreSQL ops tables so `/api/ops` and `/api/ops/performance` can recover columns added after the original migration version was recorded instead of returning 500s on live dashboards.
- Autoloop: poll recent GitHub pull requests for unresolved Copilot/Codex review threads before selecting a new autonomous idea, ask the planner to validate whether the feedback is correct, and prioritize implementing valid actionable review fixes while falling back to the normal improvement path when no review is worth changing.
- Trading/Optimizer: distinguish omitted versus explicit threshold max caps in `optimize-equity --quality`, so default caps still widen for quality sweeps while explicit `2e-2` CLI or top-combo wrapper env overrides remain tight; wrapper run-start logs now show requested and effective threshold ranges.
- Trading: fail closed on malformed fresh-entry position-size bounds by requiring max/min sizing caps to be finite and non-negative before a new backtest entry can open; valid zero caps/floors and valid minimum-size equality remain supported, and tighter valid caps cannot increase realized entry exposure.
- Bot/Live Trading: reconcile Coinbase live market orders with parsed order status, filled base size, executed quote value, and a bounded order-id lookup before updating bot position state, so successful Coinbase fills no longer look like unknown unfilled live orders.
- Bot/Live Trading: make duplicate `/bot/start` requests idempotent by returning existing bot status for already-running or already-starting symbols, make explicit `botSymbols` / `binanceSymbol` payloads take precedence over `TRADER_BOT_SYMBOLS`, skip already-active symbols in queued starts before waiting on tenant stability, and redact Binance API/listen-key details from listen-key HTTP exception logs.
- Autoloop: reject patch/diff marker payloads in model file-change `content` before writing files, so malformed patch plans fail cleanly instead of corrupting tracked source files with apply-patch text.
- Autoloop: reject prose edit-instruction payloads in model file-change `content`, so replacement instructions cannot be written into source files as if they were complete file contents.
- Trading: add backtest cost-attribution JSON with gross/net equity surfaces, realized fee/slippage/spread/funding totals, total realized cost, and a residual proving gross minus realized costs equals net for the emitted run; the gross surface is documented as a realized-run attribution/reconciliation curve, not a no-cost counterfactual replay.
- Bot/Live Trading: prevent queued `/bot/start` requests from waiting forever on below-threshold transient order errors; halted bots and order errors that reach `--max-order-errors` still block queued starts.
- Bot/Live Trading: skip `TRADER_BOT_DISABLED_SYMBOLS` (default `MATICUSDT`) for live auto-start and `/bot/start` batches so closed/delisted symbols do not churn the start queue.
- Ops/API: make outbound state-sync top-combo pushes compact by default, avoid synchronous re-posting while handling inbound `/state/sync` imports, and add `TRADER_STATE_SYNC_TIMEOUT_SEC` (default 15 seconds, capped at 300) for slower sync targets.
- Build: keep the main `trader-hs` API executable at `-O0`, matching the production Docker image and avoiding pathological GHC 9.4 optimized `Main.hs` compile times in local/CI verification.
- Bot/Live Trading: keep live volatility-confidence `block` and `allow-exit-only` decisions reduce-only by carrying the pre-gate direction into the close-direction hint, so an existing same-side bot position is held/reduced like the backtester instead of being flattened just because new entries are blocked.
- Trading: fail closed on malformed conformal calibration residual evidence by requiring `Trader.Predictors.Conformal.fitConformal` to receive a non-empty residual sample where every residual is finite and non-negative; empty samples or any negative, `NaN`, or infinite residual now produce an unavailable unbounded interval with no sigma estimate instead of a tight filtered band.
- Trading/Formal: document the conformal-calibration validity and monotone interval-width invariant in `FORMAL_METHODS.md`, including conservative handling for empty/malformed evidence and the valid zero-residual boundary.
- Trading: fail closed on malformed quantile prediction intervals by requiring `Trader.Predictors.Quantile.predictQuantiles` to emit usable intervals only from finite ordered q10/q50/q90 evidence; empty or dimension-invalid models, non-finite quantiles, and inverted q10/q90 bounds now return unavailable quantiles instead of a sorted or misleading interval.
- Trading/Formal: document the quantile-interval admissibility invariant in `FORMAL_METHODS.md`, including valid q10/q90 equality, unavailable malformed evidence, and monotone non-narrowing under wider quantile spread.
- Trading: fail closed on malformed volatility-confidence gate evidence by requiring enabled `Trader.VolConfGate` presets to receive present finite volatility within `[0,2]` and provided finite confidence within `[0,1]`; malformed volatility or malformed provided confidence now yields exit-only behavior instead of being normalized into an entry-permissive gate state.
- Trading/Formal: document the volatility-confidence gate validity invariant in `FORMAL_METHODS.md` and bind it to bounded Haskell regression coverage for malformed volatility, malformed confidence, valid boundary equality, and monotonicity under stricter confidence/volatility requirements.
- Trading: wire the fee-aware fresh-entry buffer into the actual backtest simulator and live latest-signal decision path after final entry-size overlays, so threshold/headroom-valid signals now remain flat when their modeled edge does not clear the sized round-trip cost floor.
- Autoloop: make the logical-correctness loop explicitly audit the Haskell trading algorithm first, then implement backend correctness improvements with tests/formal notes and targeted verification where practical.
- Web: make the Vite dev proxy load the repo-root `.env` as well as `haskell/web/.env*`, so direct `npm run dev` uses the same `TRADER_API_TOKEN` path as the local helper scripts.
- Deploy: align the Fly backend Docker builder with the repo-pinned GHC 9.4.8 toolchain so fresh Hackage indexes do not select dependencies incompatible with the obsolete GHC 8.10 image.
- Trading/Optimizer: model `--kelly-lite-sizing` inside `EnsembleConfig` backtests and optimizer sweeps using the same fraction/floor/cap rule as live sizing, closing a backtest/live exposure mismatch for Kelly-lite configs. Backtest text and JSON output now also show Kelly-lite realized average exposure versus a no-Kelly replay of the same config, and `optimize-equity` plus the scorecard can reject Kelly-lite trials whose realized/uncapped exposure ratio remains too high or whose uncapped exposure baseline is zero. The top-combo optimizer wrapper now exposes Kelly-lite sampling and exposure-contract knobs through environment variables, filters out lookback/max-point-infeasible intervals before launching a run, and passes `QUALITY_MIN_TRIALS`, `QUALITY_MAX_EPOCHS`, and `QUALITY_MAX_HIDDEN_SIZE` so short quality-mode production-candidate sweeps stay explicit. Quality mode now preserves explicit `--bars-min/--bars-max` bounds instead of silently resetting fixed-bar sweeps to auto-sized histories, `NOOPT=1` makes the wrapper build/use matching no-optimization `optimize-equity` and `trader-hs` binaries for fast audit loops, short fixed-bar audits that produce only `activityCount<N` skips now retry at the deeper `ACTIVITY_RETRY_BARS` counts before giving up, deepest activity-only failures get one optional `ACTIVITY_RECOVERY=1` lower-threshold/shorter-hold retry, deepest zero-activity neutral diagnostics get one optional `NEUTRAL_RECOVERY=1` method-mix retry toward edge methods, `NEUTRAL_RECOVERY_TIMEOUT_SEC` gives the final edge sweep a separate timeout budget, `--quality` preserves explicit zero method weights for recovery sweeps, and failed no-eligible attempts are summarized with filter-reason counts plus activity diagnostics instead of disappearing from `summary.json`.
- Observability/Ops: reduce production ops table growth by defaulting high-volume `binance.request` persistence off, logging `bot.status` ops snapshots every 5 minutes by default, and adding `TRADER_OPS_DISABLED_KINDS`, `TRADER_BINANCE_REQUEST_OPS_ENABLED`, and `TRADER_BOT_STATUS_LOG_INTERVAL_MS` controls.
- Observability/Ops: add `scripts/check-fly-postgres-disk.mjs` plus `npm run monitor:fly-postgres-disk` for scheduled Fly Postgres disk-capacity checks and optional warning/critical webhook alerts.
- Bot/Live Trading: cap live-bot restart/adoption bursts by defaulting auto-start to 3 target bots, 0 startup-phase top-combo bots, 1 new worker per poll cycle, and at most 3 symbols per `/bot/start` request; tune `TRADER_BOT_TOP_COMBO_BOTS`, `TRADER_BOT_TOP_COMBO_BOTS_STARTUP`, `TRADER_BOT_AUTOSTART_MAX_BOTS`, `TRADER_BOT_AUTOSTART_MAX_STARTS_PER_CYCLE`, and `TRADER_BOT_START_MAX_SYMBOLS` for larger deployments.
- Bot/Live Trading: `/bot/start` now queues symbols beyond `TRADER_BOT_START_MAX_SYMBOLS` and starts them one at a time only after the tenant's current bots are stable (not starting, halted, errored, or carrying order-error/risk-limit violations), instead of reporting the remaining symbols as immediate deferred errors. Queued starts now fail the remaining queue after `TRADER_BOT_START_QUEUE_MAX_WAIT_SEC` seconds by default, with `0` preserving an unbounded wait.
- Web: pin `haskell/web` `react` and `react-dom` to the same exact runtime version so fresh installs cannot ship a mismatched React pair that crashes the app at startup.
- Trading/Observability: restore the live `Trader.SignalGates` 24-bar directionality gate behind `Main` and align `haskell/scripts/review_bot_day.py` with the same additive-return weak-band contract, including side-aware `entry_or_add` zScore vetoes for weak long/short entries.
- Bot/Live Trading: restore closed-trade performance memory from the latest saved bot snapshot on bot restart when the symbol, market, interval, and method still match, so rolling expectancy/win-rate/profit-factor gates, adaptive filter tightening, loss-streak tracking, and the first post-restart decision resume from prior wins and losses instead of resetting to an empty session.
- Trading: fail closed on negative fresh-entry open thresholds by requiring the open threshold consumed by the spike, headroom, and fee-buffer gates to stay finite and non-negative, so corrupted `--open-threshold` or legacy `--threshold` inputs can no longer relax the fresh-entry deadband to zero.
- Trading/Formal: document the fresh-entry threshold-validity invariant in `FORMAL_METHODS.md` and bind it to bounded Haskell coverage for negative-threshold rejection plus zero-threshold valid-boundary behavior.
- Trading: fail closed on negative fresh-entry fee floors by requiring the round-trip fee floor consumed by `signalEntryFeeBufferOk` to stay finite and non-negative, so corrupted negative fee inputs can no longer relax the marginal-entry gate back to the zero-fee boundary.
- Trading/Formal: update `FORMAL_METHODS.md` and bounded Haskell regressions so negative fee floors are rejected both at the pure `SignalGates` contract and through the `mkEntryGateState` fresh-entry integration witness.
- Observability: extend `haskell/scripts/review_bot_day.py` with interval-aware cutoff freshness diagnostics, per-symbol stale-snapshot detection, and a compact latest pre-cutoff `latestSignal.action` census in both JSON and markdown output.
- Observability/Formal: daily review non-directional accounting now explicitly treats same-day order flow as `entry_or_add` versus `exit_or_flatten`, keeps `nonDirectionalOrderAttempts` limited to entry/add events, and documents that adopted-position carry-management exits are reported separately from fresh weak-directionality entry misses.
- Autoloop: stop blocking the forever runner on merged local branches that are still attached to Git worktrees, and add `hlint` verification plus direct log-driven HLint auto-fixes for editable Haskell files.
- Autoloop: prune stale `git worktree` metadata before merged-ref cleanup, and move dirty-cycle rescue/checkpoint commits onto dedicated `autoloop/recovery/*` and `autoloop/checkpoint/*` branches instead of pushing failed `WIP` snapshots onto `main`.
- Autoloop/CI: require `AUTOLOOP_PUSH_TOKEN` in the GitHub-hosted autoloop workflow and remove the `github.token` fallback plus `AUTOLOOP_SKIP_CI_WAIT`, so post-push GitHub Actions polling is mandatory there.
- Autoloop/CI: install a pinned prebuilt `fourmolu` binary in the GitHub-hosted autoloop workflow instead of compiling `fourmolu` from source on each run.
- Developer Experience/AI: add a repo-root `scripts/verify.sh` wrapper plus `npm run verify*` aliases so local agents and humans can run the same Haskell, web, smoke, and automation checks without reconstructing CI by hand.
- Developer Experience/AI: add CI coverage for the root autoloop regression test, document local AI automation environment variables, and pin local Node/GHC/Cabal versions with `.nvmrc` and `.tool-versions`.
- Trading: make weak-band fresh-entry gating side-symmetric via one shared signed additive-path zScore rule in the 24-bar directionality review band, so weak long entries require `zScore >= 0.75`, weak short entries require `zScore <= -0.75`, exact signed-boundary equality remains admissible, and opposite-signed, zero, missing, or non-finite weak-band scores fail closed for the requested side while malformed saved HMM tuples still block conservatively.
- Trading/Formal: document the weak-band signed-zScore monotonicity invariant in `FORMAL_METHODS.md` and extend bounded Haskell regressions with side-specific boundary, opposite-sign, zero, malformed-input, and monotonicity coverage.
- Trading: fail closed on fresh-entry directionality when the saved or live 24-bar efficiency sample is non-finite or outside `[0,1]`, so malformed efficiency cannot fall through the strong-band no-regime path before the existing `0.40` review-band and `0.25` chop cutoffs are applied.
- Trading/Formal: document the efficiency-range invariant in `FORMAL_METHODS.md` and extend bounded Haskell regressions with negative/above-1 efficiency fail-closed coverage plus the explicit monotone `1.0 -> 0.40 -> 0.25` admissibility ladder.
- Trading: treat negative or non-finite `--regime-bank-hysteresis` as malformed during weak-band fresh-entry HMM review, so otherwise marginal weak-band entries fail closed as `NON_DIRECTIONAL_MALFORMED` instead of reopening through a permissive MR-gap comparison.
- Trading/Formal: document the weak-band regime-bank hysteresis validity and monotone MR-gap veto in `FORMAL_METHODS.md` and add bounded Haskell regressions for malformed-hysteresis rejection plus the hysteresis-gap ladder.
- Trading: add a fee-aware fail-closed entry gate for marginal signals, so entries now require the method-selected absolute edge to clear both the existing adjusted headroom requirement and an explicit round-trip fee floor (`2 * fee`); non-finite fee or edge inputs are blocked conservatively.
- Trading/Formal: document the fee-aware entry-gate contract in `FORMAL_METHODS.md` and add bounded Haskell tests covering fee monotonicity, edge monotonicity, and malformed-input fail-closed behavior.
- Trading: restore the fresh-entry edge-spike veto as a maximum-edge sanity gate: entries now require finite non-negative edge samples no greater than `min(4 * active open-threshold, 0.5)`, so absurd one-bar forecast edges are blocked as `EDGE_SPIKE` while the separate headroom and fee-buffer gates still enforce minimum edge.
- Trading/Tests: add bounded Haskell tests covering spike-cap equality, strict-above-cap rejection, malformed-input rejection, and ETC-style spike replay coverage.
- Trading: derive low-directionality efficiency and z-score from the additive per-bar return path, so clean monotonic trend windows stay directional instead of being misclassified as malformed when the compounded endpoint return slightly exceeds the summed simple-return path.
