- `--open-threshold 0.002` (or legacy `--threshold`) entry/open direction threshold (fractional deadband)
  - `--max-hold-bars 0` now passes validation and disables forced max-hold exits as documented. Optimizer close-timing retunes also apply `maxHoldBars` recommendations only when the formal support, positive-lift, domain, and deadband guards accept them, and close-timing metrics report the accepted applied hold limit.
  - Deployment: `deploy/hetzner/` now contains the cost-effective always-on production path for the stateful API: split trading/research Hetzner servers, Docker Compose for `trader-hs`, local Postgres, Caddy TLS/path isolation, browser Basic Auth with same-origin API-token injection, state sync, systemd restart/watchdog timers, and daily backup hooks.
  - Hetzner research and trading now derive their tenant key from the same shared Binance credential pair when `HETZNER_BINANCE_API_KEY` / `HETZNER_BINANCE_API_SECRET` are configured for CI deploys; `TRADER_STATE_SYNC_TENANT_KEY` is left empty so the backend uses the derived `binance:<hash>` tenant instead of the old `trader-prod` placeholder.
  - Hetzner CI deploys retry transient SSH failures with bounded connect timeouts, and both trading and research boxes are mandatory so each advances to the latest green commit. Manual Hetzner dispatch redeploys the latest successful CI commit for the selected branch instead of the branch tip. Deploy sync excludes local Cabal/Stack/Python/temp caches to avoid multi-GB transfers.
  - The web UI automatically fits an over-large lookback window/override down to the available capped bars when it cannot fix the request by increasing `bars`.
  - Operation logs now include `serverId`, `serverRole`, and `serverProvider` so rows identify whether they ran locally, on Fly, or on Hetzner. Live-bot order rows also include opener/closer server attribution when a persisted position origin is available.
  - Scheduled top-combo backtests now always include combos whose backtest freshness is older than 3 days, regardless of their current rank. Sub-1.0 refreshed performance is dropped from the top-combos payload with a tombstone so stale DB/S3/state-sync replicas cannot re-add the old score; zero-trade windows only refresh the timestamp and leave optimizer metrics intact. The refresh age and prune threshold are tunable with `TRADER_TOP_COMBOS_BACKTEST_STALE_DAYS` and `TRADER_TOP_COMBOS_BACKTEST_PRUNE_FINAL_EQUITY_FLOOR`, preserving defaults of `3` and `1.0`.
  - Live bot top-combo adoption refuses combos whose latest discovery/backtest freshness is older than 14 days or missing. Running bots hold flat or exit stale combo exposure until a fresh deployable combo is found, and auto optimizer discovery recovery runs when no fresh deployable combo exists for the active symbol/interval.
  - Auto optimizer discovery-recovery open/close threshold ranges are tunable with `TRADER_OPTIMIZER_DISCOVERY_OPEN_THRESHOLD_MIN/MAX` and `TRADER_OPTIMIZER_DISCOVERY_CLOSE_THRESHOLD_MIN/MAX`, preserving the previous recovery defaults while allowing production nodes to widen or tighten no-eligible retry searches without editing code.
  - Server-side auto optimizer scope selection now supports default-on `TRADER_OPTIMIZER_AUTO_CAPPED_LOOKBACKS`, which derives the largest split-feasible lookback for intervals whose configured `TRADER_OPTIMIZER_LOOKBACK_WINDOWS` would exceed `TRADER_OPTIMIZER_MAX_POINTS`, keeping short-interval research scopes active under tight fetch caps.
  - Survivor crossover parent gates are tunable with `--survivor-parent-activity-floor` and `--survivor-parent-annualized-return-floor`, survivor refresh can bias exploitation toward newly discovered high-edge trials with `--survivor-edge-weight` (default `0.10`), and `--survivor-rank-bias` (default `1.5`, `0` disables) spends more perturbation/crossover draws on the top edge-adjusted survivors. API optimizer runs accept `survivorParentActivityFloor`, `survivorParentAnnualizedReturnFloor`, `survivorEdgeWeight`, and `survivorRankBias`; the advanced web optimizer form exposes the controls, server-launched runs can set `TRADER_OPTIMIZER_SURVIVOR_PARENT_*`, `TRADER_OPTIMIZER_SURVIVOR_EDGE_WEIGHT`, `TRADER_OPTIMIZER_SURVIVOR_RANK_BIAS`, and `TRADER_OPTIMIZER_EDGE_SCORE_*`, and `haskell/scripts/run_optimize_equity_top5.sh` forwards matching `SURVIVOR_*` and `EDGE_SCORE_*` env settings. Defaults preserve the previous `opCount > 5`, `annualizedReturn > 1`, and edge-score formula while focusing survivor exploitation toward stronger edge evidence.
  - Top-combo ranking now sinks combos below the live-adoption trade-count evidence floor, keeping one- or two-trade annualized-return outliers from blocking newly discovered deployable candidates.
  - Top-combo payloads now dedupe source/null-equivalent strategy rows and annotate each row with `processing.tier`, `processing.validatedScore`, `processing.rankScore`, and missing-evidence reasons. Deployable walk-forward-validated combos rank ahead of unvalidated candidates, while raw and quarantined rows remain visible below them. `merge-top-combos` exposes the scoring coefficients with `--score-*` flags, including opt-in recency ranking through `--score-freshness-half-life-days` and `--score-freshness-floor-multiplier`; the server-side optimizer merge forwards matching `TRADER_TOP_COMBO_SCORE_*` env overrides.
  - Optimizer JSONL records and generated top-combo payloads now carry `metrics.walkForwardSummary` from backtest walk-forward output, so fresh trials that pass the walk-forward Sharpe floor can be ranked as deployable instead of remaining candidates with missing evidence.
  - Live top-combo adoption now exposes `--adoption-max-position-size-cap` (default `0.25`) to tune the safety cap applied to adopted `maxPositionSize` values from legacy leaderboard combos. The evidence gates are also tunable with `--adoption-min-trade-count` (default `20`) and `--adoption-min-wf-sharpe-mean` (default `0.3`) so deployments can optimize the trade-count and walk-forward Sharpe adoption floors without code edits.
  - Live fill-slippage calibration is tunable with `--cost-calibration-min-observations`, `--cost-calibration-shrinkage-obs`, `--cost-calibration-window`, `--cost-calibration-floor-factor`, `--cost-calibration-max-per-side`, and `--cost-calibration-outlier-bound`, with matching API camelCase fields and `/bot/status` reporting. Defaults preserve the previous `8`, `16`, `64`, `0.25`, `0.01`, and `0.05` calibration behavior while allowing ROI analysis to tune how quickly real fills change live cost gates.
  - Binance account trades now show Open Instance and Close Instance attribution (`laptop`, `hetzner`, or `fly`) by matching exchange fills back to persisted order operations, while keeping the existing origin/close IP columns for low-level diagnostics. Non-zero realized-PNL fills whose opener is outside the visible account-trade window are treated as historical closes, so they do not create phantom reverse lots or steal attribution from later visible trades.
  - Binance account trades also include interval-scored Max PNL and Max Close columns. For paired opening fills, `/binance/trades` fetches the active interval's candles and records the best gross PNL reachable between the open time and `open + 2 * actual trade duration`, using candle highs for longs and lows for shorts. If a paired trade's candle fetch is unavailable, the scorer falls back to the known entry and actual exit fill prices; if a visible close fill's opener predates the fetched trade window, it records a conservative realized-PNL fallback at the actual close timestamp instead of leaving Max PNL / Max Close blank.
  - Opt-in live herd confirmation is available with `--prediction-market-herd`: latest-signal, trade, and live-bot fresh entries query Polymarket crypto up/down markets for the corresponding symbol and nearest supported interval (`15m`, `1h`, `4h`, `1d`). Same-side odds boost reported confidence and entry size up to `--prediction-market-herd-max-boost`; opposing or below-floor odds hold the fresh entry as `HOLD (PREDICTION_MARKET_HERD)`. Tune with `--prediction-market-herd-min-probability`, `--prediction-market-herd-min-volume`, `--prediction-market-herd-limit`, `--prediction-market-herd-fresh-ttl-sec`, `--prediction-market-herd-stale-ttl-sec`, `--prediction-market-herd-score-base`, `--prediction-market-herd-interval-match-bonus`, `--prediction-market-herd-time-decay-bonus`, `--prediction-market-herd-past-end-penalty`, and `--prediction-market-herd-volume-score-weight`.
  - `--method` now includes `meta_hedge_blend` (aliases `universal_blend` and `all_methods_blend`), an online Hedge meta-ensemble that weights the available Kalman/LSTM-derived method forecast streams by clipped realized next-bar forecast error. It reuses `--blend-hedge-eta` and `--blend-hedge-max-error`, participates in backtests, latest signals, and `--optimize-operations`, and `optimize-equity` can sample it with `--method-weight-meta-hedge-blend`.
  - `--take-profit-partial` now implements a protected-runner exit: backtests and live bots scale out at the configured take-profit, reduce the remaining exposure, move the residual stop to breakeven, and keep the remainder open for trailing or signal-based exits.
  - `--method` now includes `online_nn`, a point-in-time online neural-network strategy for live latest-signal decisions and backtests. It trains a two-hidden-layer MLP on the existing OHLCV/derived predictor inputs plus attached market context when available, including funding, open interest, basis, taker flow, Coinbase cross-exchange features, and Binance market-context features. The model predicts the next-bar price, updates after each realized bar, and uses `--lookback-bars`, `--hidden-size`, `--epochs`, `--lr`, and `--seed`; it can be threshold-swept with `--sweep-threshold` but is not part of `--optimize-operations`.
  - Coinbase cross-exchange enrichment is available for Binance latest-signal, trade, backtest, and live-bot decisions with `--cross-exchange-coinbase`, or by setting `TRADER_CROSS_EXCHANGE_COINBASE=true` for REST defaults. The system aligns same-asset Coinbase candles (for example `BTCUSDT` to `BTC-USD`) to the Binance bar grid, feeds the basis/lead-lag features into LSTM/Kalman predictors, and fails open to Binance-only when Coinbase cannot be fetched.
  - `--method` now includes first-class technical-analysis methods for live latest-signal decisions and backtests: `ta_trend` (EMA20/EMA50 with ADX/Aroon confirmation and ATR risk framing), `ta_reversion` (RSI/Stochastic/ROC/MACD with Bollinger/Keltner envelope context), `ta_breakout` (Donchian channel breakout with OBV/A-D/CMF/MFI/VPT volume-flow confirmation), `ta_best` (selects the strongest admitted TA candidate), and `ta_regime_switch` (uses ADX > 25 plus price above EMA200 for long-biased trend, narrow Bollinger bandwidth for reversion, otherwise breakout). These methods run without Kalman/LSTM model contexts and can be threshold-swept with `--sweep-threshold`; `--optimize-operations` remains limited to the existing model/fusion/router methods.
  - Technical-analysis, predictor-training, volatility-confidence gate, adaptive performance-gate, ensemble, and ROI objective-scoring coefficients that affect ROI are now CLI tunables instead of fixed constants: `--ta-entry-open-threshold`, trend ADX/ATR stop/take-profit multipliers, reversion RSI/Stochastic/envelope/stop thresholds, breakout MFI/ATR stop/take-profit multipliers, SMA-cross ATR stop/take-profit multipliers, regime-switch ADX/Bollinger-bandwidth thresholds, `--ta-best-min-confidence`, TA confidence-scoring coefficients for trend ADX/MA/Aroon, reversion ROC, breakout flow/distance, and SMA-cross spread, predictor GBDT/conformal calibration constants, `--predictor-tcn-ridge-lambda` / API `predictorTcnRidgeLambda`, quantile predictor `--predictor-quantile-*` / API `predictorQuantile*` controls, and KNN/decision-tree/transformer/HMM predictor training controls (`--predictor-knn-*`, `--predictor-decision-tree-*`, `--predictor-transformer-*`, `--predictor-hmm-iterations`) with matching API fields, volatility/confidence bucket thresholds and size multipliers, adaptive win-rate/profit-factor ramp slack, plus blend softmax/net-softmax scales, edge-blend power, smooth-softmax alpha, hedge-blend learning/error caps, divergence shrinkage, regime-switch/router/performance-regime cutoffs, bandit-router exploration, phase-cancel return/conflict/alignment coefficients, and ROI score expectancy/payback/activity/exposure evidence penalties. Optimizer/tune simulations use the same blend coefficients through `EnsembleConfig`, and the cached/latest-bar breakout paths now share the calibrated breakout take-profit multiplier.
  - Router and bandit-router same-regime evidence floors are tunable with `--router-regime-min-bars` and `--router-regime-min-fraction`; `optimize-equity` samples them with `--router-regime-min-bars-min/max` and `--router-regime-min-fraction-min/max`, API requests accept `routerRegimeMinBars` and `routerRegimeMinFraction`, top-combo JSON preserves the sampled settings, correlation-guided optimizer guesses can prefill matching ranges, the web UI exposes controls, and the top-5 wrapper forwards matching `ROUTER_REGIME_*` env ranges.
  - Optimizer-generated and prior-derived risk sizing now normalizes volatility stop/take-profit/trailing-stop multipliers as positive values, only keeps `riskPerTrade` with a fixed stop-loss, caps it at the simulator's position-size scale hard limit, and drops it for volatility-stop trials whose realized stop fraction is data-dependent, so sampled trials do not emit CLI-invalid or runaway risk sizing.
  - `Trader.ThresholdCalibration` exposes `ThresholdCalibrationConfig` plus `calibrateThresholdWithConfig`, so threshold-calibration consumers can tune the headroom divisor, fee floor, minimum sample size, and conservative/aggressive percentile boundaries while the default `calibrateThreshold` behavior remains unchanged.
  - Live bots now resume closed-trade performance memory from the latest saved bot snapshot when the symbol, market, interval, and method still match. That means rolling expectancy/win-rate/profit-factor gates, adaptive filter tightening, loss-streak tracking, and the first post-restart decision continue from prior wins and losses instead of restarting from an empty session.
  - Live Binance futures bots now default to a capital-preservation gate for new exposure: if current drawdown reaches 10%, three losses occur in a row, recent closed trades lose at least 5% over the 20-trade window, or rolling trade Sharpe turns negative once enough trades exist, fresh entries are held and reversal attempts close to flat instead of flipping. The same live-futures gate now also runs at the tenant portfolio level: a cross-symbol loss streak or aggregate recent loss blocks all fresh entries for 30 minutes, including startup/autostart entries, while preserving closed-trade memory so outcome-weighted LSTM fine-tuning and adaptive filters still learn from wins and losses. `/bot/status` reports the per-bot guard under `performance.capitalPreservation`; tune the shared thresholds with `--capital-preservation-lookback`, `--capital-preservation-min-trades`, `--capital-preservation-max-drawdown`, `--capital-preservation-max-rolling-loss`, `--capital-preservation-min-sharpe`, and `--capital-preservation-loss-streak-max`.
  - Each newly closed live trade now triggers an immediate outcome-weighted LSTM fine-tune and model persist before the next entry cycle. Winning spans reinforce the pattern, losing spans weigh harder to correct the mistake, and `bot.adjust` telemetry reports the applied outcome weight plus whether the immediate LSTM update ran.
  - The Positions page now starts with an open-position digest showing total PnL, PnL/notional, long/short exposure, estimated margin, weighted leverage, nearest liquidation distance, and largest position before the individual charts.
  - Binance kline ingestion now treats only closed candles as strategy input: the still-open current candle is discarded using the exchange close-time field, bot status includes a `marketData` freshness object, and live bots hold with explicit stale/gap actions instead of processing a stale or non-contiguous feed.
  - Live `/bot/status` responses and saved bot snapshots now include a compact server-side `decisionTrace` object. The live UI uses it to show the current bar's forecast, gate/control outcome, sizing, and order intent without re-deriving that flow in the browser.
  - Coinbase live market orders now feed parsed order id, status, filled base size, and executed quote value into the same execution reconciliation path as Binance; when a create response lacks fill evidence, the bot performs a short bounded order-id lookup before deciding whether to update local position state.
  - Binance Spot user-data streams use Binance's WebSocket API `userDataStream.subscribe.signature` flow instead of the removed REST listenKey endpoint. Futures and margin user streams still use REST listen keys; Spot keep-alive and close actions target the backend WebSocket session.
  - Fresh-entry gates require the configured open threshold to be finite and non-negative. Negative or non-finite `--open-threshold` / legacy `--threshold` values fail closed instead of being normalized to a zero deadband; a valid zero threshold remains admissible only when the explicit edge sample clears the active headroom, fee, and spike checks.
  - Fresh-entry gates now also enforce the exact feasibility boundary implied by the existing headroom and spike contract: because entry needs `edge >= 1.5 * openThreshold` while the default spike veto requires `edge <= min(1000 * openThreshold, 5.0)`, any open threshold strictly above `10/3` is impossible and is reported as `HOLD (THRESHOLD_INFEASIBLE)`. Equality at `10/3` remains admissible when the edge is exactly `5.0`.
  - Optimizer and top-combo import/export paths now reject combos with `openThreshold > 10/3` before they can be ranked or deployed.
  - Entries must also clear that deadband with at least `1.5x` edge headroom; forecasts that only barely cross the threshold are held as `HOLD (EDGE_HEADROOM)` instead of opening on a boundary-touching move.
  - Entries are also vetoed when the method-selected absolute edge does not clear the active headroom requirement plus the modeled round-trip cost floor (fees, slippage, spread, and configured fixed/minimum costs), which blocks low-margin trades that only look profitable before costs. The fee floor must be finite and non-negative; negative or non-finite fee inputs and missing or non-finite edge samples fail closed. Backtests and live latest-signal decisions now both apply this fee-buffer gate on the actual fresh-entry path after final entry-size overlays, so fixed/minimum costs cannot be understated by checking a larger pre-overlay size.
  - `--max-position-size` must be strictly positive (`> 0`) and at most `10` (or `5` for spot/margin markets). Zero and negative values are rejected at CLI validation time, matching the existing `--initial-balance` and `--trailing-stop` strict-positive contracts.
  - Fresh-entry sizing now requires the configured maximum and minimum position-size bounds to be finite and non-negative before a new position can open. Negative, `NaN`, or infinite cap/floor evidence fails closed as a no-entry sizing state; valid zero caps and zero floors remain explicit boundaries, and equality at a valid minimum size remains admissible.
    - Backtest JSON now includes cost-attribution accounting under `costAttribution` and `costs.attribution`, with gross and net equity curves, realized fee/slippage/spread/funding totals, total realized cost, and a consistency residual proving `gross - realized costs = net` for the emitted run. Here, `gross` is the attribution/reconciliation curve obtained by adding cumulative realized costs back to `net` for that realized run; it is not a separately replayed no-cost counterfactual equity curve or return series, which would generally differ because compounding would occur on a higher capital base.
  - Volatility-confidence gate presets now require volatility evidence to be present, finite, non-negative, and at most the configured evidence max (`2.0` by default); missing, negative, non-finite, or above-range volatility evidence fails closed as exit-only. Provided confidence evidence must be finite and within `[0,1]`; malformed confidence also fails closed as exit-only, while missing confidence remains weak entry-blocking evidence.
  - Close timing analyzer: run `cd haskell && cabal run analyze-close-timing -- --input ../data/close_timing_input.json --output ../data/close_timing_report.json` against a JSON array of combos shaped as `{ "comboId": "...", "prices": [...], "trades": [{"entryIndex": ta, "exitIndex": tc, "entryPrice": p_ta, "side": 1|-1}] }`. It computes per-trade `tm = argmax PNL(t)` on `t in [ta, ta + 2*(tc-ta)]`, reports robust per-combo `tm/(tc-ta)` stats and lift, and emits fail-closed max-hold retune evidence.
  - Live bots also learn close timing from realized positions: when a combo-tagged trade closes, the bot records the elapsed bars from open to its maximum favorable PNL. Same-tenant bots running that combo use supported live evidence to shorten the effective max-hold horizon and close with `MAX_PNL_TIMING`; runtime evidence does not widen an existing max-hold cap.
  - Conformal prediction calibration now requires a non-empty residual sample where every residual is finite and non-negative. Empty samples or any malformed residual evidence, including negative, `NaN`, or infinite values mixed with otherwise valid residuals, produce an unavailable unbounded interval with no sigma/confidence estimate instead of a tight filtered band. Valid zero residuals remain admissible.
  - Quantile prediction intervals now require finite ordered q10/q50/q90 evidence from a dimension-valid model. Empty or dimension-invalid models, non-finite quantile predictions, and inverted q10/q90 bounds return unavailable quantiles instead of silently sorting or emitting a tight misleading interval. Exact equal q10/q90 bounds remain valid, but produce no sigma estimate.
  - Entries from known scaled LSTM-only models now report excessive method-selected edge as an `EDGE_SPIKE` audit warning instead of using it as a live entry veto; unscaled models still treat strict-above-cap spikes as a hard gate. This spike check remains separate from the minimum-edge headroom and fee-buffer gates.
  - The shared SMA trend confirmation gate is now slightly less strict: live and backtest entries may stay trend-aligned while price sits within half of the active open-threshold below/above the SMA, capped at a 1% slack band, instead of requiring an exact side-of-SMA print.
  - Fresh-entry signal-gate coefficients can now be tuned with `--signal-entry-edge-headroom-mult`, `--signal-entry-edge-spike-mult`, `--signal-entry-edge-spike-cap`, `--signal-entry-edge-spike-consecutive-limit`, `--signal-trend-slack-mult`, `--signal-trend-slack-cap`, `--signal-directionality-lookback-bars`, `--signal-directionality-chop-efficiency-max`, `--signal-directionality-mr-efficiency-max`, `--signal-directionality-weak-z-min`, and `--signal-predictor-tracking-floor`, so edge-headroom, spike sanity, audit escalation, SMA trend slack, directionality, and predictor-liveness rules can be optimized without code edits.
  - In `optimize-equity --quality`, omitted default threshold max caps still widen to `5e-2`, while explicitly supplied `--open-threshold-max 2e-2` / `--close-threshold-max 2e-2` and wrapper `OPEN_THRESHOLD_MAX=2e-2` / `CLOSE_THRESHOLD_MAX=2e-2` settings remain tight. The top-combo wrapper logs both requested and effective threshold ranges at run start.
  - Backtests and optimizer sweeps now model `--kelly-lite-sizing` with the same `--kelly-lite-fraction`, floor, and cap used by live sizing, so selected configs no longer ignore Kelly-lite exposure changes during historical scoring. When Kelly-lite is enabled, backtest text and JSON output also report realized average exposure versus the same backtest replayed without Kelly-lite sizing; `optimize-equity` and the scorecard can now require Kelly-lite trials to reduce realized exposure materially before they remain eligible, and zero-uncapped-exposure Kelly-lite rows fail that exposure contract instead of passing ratio checks as no-op reductions. `haskell/scripts/run_optimize_equity_top5.sh` exposes the same knobs through `P_KELLY_LITE_SIZING`, `KELLY_LITE_FRACTION_MIN/MAX`, `KELLY_LITE_FLOOR_MIN/MAX`, `KELLY_LITE_CAP_MIN/MAX`, `MIN_KELLY_LITE_EXPOSURE_REDUCTION`, and `MAX_KELLY_LITE_EXPOSURE_RATIO`; the wrapper also passes `LOOKBACK_WINDOW`, filters out intervals that would exceed `TRADER_OPTIMIZER_MAX_POINTS`, and uses `QUALITY_MIN_TRIALS`, `QUALITY_MAX_EPOCHS`, and `QUALITY_MAX_HIDDEN_SIZE` so short quality-mode audit sweeps can keep their requested trial/model budget explicit. Quality mode now also preserves explicit `--bars-min/--bars-max` bounds instead of silently resetting fixed-bar sweeps to auto-sized histories, `NOOPT=1` makes the wrapper build/use matching no-optimization `optimize-equity` and `trader-hs` binaries for fast audit loops, and short fixed-bar audits that produce only `activityCount<N` skips now retry at the deeper `ACTIVITY_RETRY_BARS` counts before giving up. When the deepest history still fails only on activity count, `ACTIVITY_RECOVERY=1` runs one lower-threshold/shorter-hold recovery attempt using the `ACTIVITY_RECOVERY_*` range variables while preserving explicit tight threshold caps under `--quality`. If that recovery still produces only zero-activity neutral latest-signal diagnostics, `NEUTRAL_RECOVERY=1` runs one bounded method-mix retry using the `NEUTRAL_RECOVERY_METHOD_WEIGHT_*` variables, shifting sampling toward edge methods without relaxing eligibility gates; `NEUTRAL_RECOVERY_TIMEOUT_SEC` gives that slower final edge sweep a separate timeout budget that defaults to at least 90 seconds, and `--quality` now preserves explicit zero method weights so that retry can exclude neutral-heavy methods. Failed no-eligible attempts are also written into `summary.json` with status, bar count, trial count, filter-reason counts, and activity diagnostics so audit automation can see whether trials were inactive because of zero trades, sparse trades, or latest-signal HOLD gates.
  - The live `Trader.SignalGates` path now applies the real 24-bar directionality gate instead of the old compatibility shim. Fresh-entry directionality first requires the 24-bar efficiency sample to be finite and within `[0,1]`; missing directionality snapshots plus `NaN`, negative, or above-1 efficiency values are vetoed conservatively instead of being treated as strong-band directional evidence. Entries are now vetoed as `NON_DIRECTIONAL_CHOP` only when 24-bar price efficiency is `<= 0.08`, and as `NON_DIRECTIONAL_MR` when efficiency stays `<= 0.35` while saved HMM regime probabilities are clearly mean-reversion-dominated (using the existing `--regime-bank-hysteresis` gap). In that same weak-directionality review band, `--regime-bank-hysteresis` itself must be finite and non-negative; negative or non-finite values now fail closed as `NON_DIRECTIONAL_MALFORMED` instead of being normalized into a looser MR comparison. In that same weak-directionality review band, fresh long and short entries now share one signed additive-path confirmation rule: the requested side is admissible only when the signed weak-band `zScore` is finite and at least `0.50` (equivalently `zScore >= 0.50` for longs and `zScore <= -0.50` for shorts). Exact equality at the signed boundary remains admissible, while opposite-signed, zero, missing, or non-finite weak-band scores fail closed for the requested side. Saved HMM regime probabilities still fail closed as `NON_DIRECTIONAL_MALFORMED` when a tuple is present but any component is missing, non-finite, outside `[0,1]`, or when the three-probability mass is non-positive or drifts by more than `1e-3` from `1`; absent regime probabilities now skip the MR comparison and rely on the signed weak-band `zScore` instead of being mislabeled as malformed.
  - Directionality efficiency and z-score are derived from the additive per-bar return path, so clean monotonic trend windows remain directional instead of being misclassified as malformed when the compounded endpoint return slightly exceeds the summed simple-return path.
  - `haskell/scripts/review_bot_day.py` now emits additive cutoff observability in both JSON and markdown: interval-aware stale-snapshot diagnostics (`latestSnapshotAgeMs`, per-symbol cutoff freshness rows, `staleSnapshotsAtCutoff`) plus a compact latest pre-cutoff `latestSignal.action` census so zero-trade review windows still show stale snapshots and dominant HOLD reasons.
  - `haskell/scripts/review_bot_day.py` now also audits cutoff-time latest signals conservatively before any hypothetical fresh entry: it reports per-symbol measurable edge availability/source, threshold and `1.5x` headroom ratios, malformed directionality/regime fail-closed flags, and aggregate cutoff counts so no-trade days show whether the bot stayed flat because signals were truly neutral, below threshold/headroom, or malformed. For legacy snapshots written before the missing-regime weak-band fix, the audit rebuilds that directionality classification from prices plus side evidence so `regimes: null` no longer inflates malformed-input counts.
  - When `haskell/scripts/review_bot_day.py` is run for the current local date without `--end-local`, it now reviews through the current local time instead of future local midnight; historical dates still default to the full local day.
  - `haskell/scripts/review_bot_day.py` also keeps low-directionality review accounting aligned with the live gate: same-day order flow is partitioned as `entry_or_add` versus `exit_or_flatten`, `nonDirectionalOrderAttempts` counts only entry/add flow, weak-band entry/add reviews use the same signed additive-path zScore rule as live (`zScore >= 0.50` for longs, `zScore <= -0.50` for shorts), and carry-management exits from existing or adopted positions stay in the separate `nonDirectionalExitOrFlattenEvents` / `nonDirectionalUnknownRoleEvents` review buckets.
  - Ops persistence now defaults high-volume Binance REST request audit rows (`binance.request`) off, persists `bot.status` ops snapshots every 5 minutes by default, and supports `TRADER_OPS_DISABLED_KINDS` plus `TRADER_BINANCE_REQUEST_OPS_ENABLED` / `TRADER_BOT_STATUS_LOG_INTERVAL_MS` for production disk-growth control.
  - Binance trade attribution now separates requester origin IP from executor IP. Order ops stamp `executorIp` from the instance's Binance/public egress IP (or `TRADER_BINANCE_EGRESS_IP` when set), and the web trades tables use executor IPs for Open IP / Close IP instead of showing the browser/API caller's request IP.
  - Fly Postgres disk monitoring is available with `npm run monitor:fly-postgres-disk -- --app tdf-hq-db --warn 75 --critical 85`; set `TRADER_DISK_ALERT_WEBHOOK_URL` to post warning/critical JSON alerts from cron, GitHub Actions, or another scheduler.
  - The web UI and server-side API normalization now auto-raise `bars` when the configured lookback needs more exchange history than the current explicit/default bar count. The backtest configuration also auto-adjusts bars and, when capped history prevents preserving the current holdout, lowers the backtest ratio enough to keep `lookback + 1` training bars and a valid holdout. That includes live `bot/start`, `signal`, and `trade` requests plus bot starts that inherit tighter `bars` from top-combo payloads, so feasible requests no longer fail with split/lookback sizing errors.
  - The web UI header now has explicit Config and Sections menus. Selecting a menu item shows only that one page/section, including output pages such as Overview, Live bot, Backtest, Data log, and Optimizer Combos.
  - Optimizer Combos now includes parameter-to-ROI correlation charts, scoped to the active combo filters, so top numeric/boolean combo knobs can be compared against realized ROI. The same filtered correlations can prefill optimizer run ranges and advanced extra JSON knobs as educated guesses for the next search.
  - Threshold sweep resolution is now tunable with `--tune-max-threshold-candidates` (default `60`) for both `trader-hs` and `optimize-equity`; set it to `0` to evaluate only the configured base thresholds. Server-launched optimizer runs also accept API `tuneMaxThresholdCandidates` and `TRADER_OPTIMIZER_TUNE_MAX_THRESHOLD_CANDIDATES` so deployments can trade runtime against threshold-search granularity without rebuilding.
  - The `cross_sectional_momentum` / `xsmom` method uses market-residual momentum adjusted by funding, basis, taker-flow, and open-interest context. It is available to backtests, live signals, optimizer sweeps, API optimizer requests, and web method controls; tune its optimizer sampling through `TRADER_OPTIMIZER_METHOD_WEIGHT_CROSS_SECTIONAL_MOMENTUM` or the top-5 wrapper's `METHOD_WEIGHT_CROSS_SECTIONAL_MOMENTUM` / `NEUTRAL_RECOVERY_METHOD_WEIGHT_CROSS_SECTIONAL_MOMENTUM`.
  - API optimizer requests accept `methodWeight11`, `methodWeight10`, and `methodWeight01` in addition to the newer method-family weights, so advanced optimizer payloads can disable Kalman/LSTM families when focusing a run on alternatives such as `cross_sectional_momentum`. Exchange-backed optimizer runs also validate `barsMin` and `barsMax` against the live 1000-bar execution cap before launching the search.
  - Sensor residual variance adaptation is now tunable with `--sensor-variance-ewma-alpha` (default `0.05`) for `trader-hs` and `optimize-equity`; the learned residual variance now floors model-reported sigma before Kalman fusion. Conservative Kalman fusion can also be tuned with `--kalman-sensor-correlation-inflation`, `--kalman-innovation-inflation-threshold`, and `--kalman-innovation-inflation-max`; API requests accept matching camelCase fields, and server-launched optimizer runs can set `TRADER_OPTIMIZER_SENSOR_VARIANCE_EWMA_ALPHA` plus `TRADER_OPTIMIZER_KALMAN_*` env controls, so Kalman sensor weighting can be tuned for ROI without code changes. The `kalman_physics_error` sample window, holdout split, volume-adjusted close-bias coefficients, candidate-selection validation split, and optional fixed GBDT correction candidate can now be tuned with `--kalman-physics-bars` (default `1000`), `--kalman-physics-backtest-ratio` (default `0.3`), `--kalman-physics-volume-ewma-alpha` (default `0.1`), `--kalman-physics-volume-signal-clamp` (default `3`), `--kalman-physics-close-bias-scale` (default `0.05`), `--kalman-physics-candidate-validation-ratio` (default `0.2`), `--kalman-physics-small-sample-validation-ratio` (default `1/3`), `--kalman-physics-candidate-trees` (default `0`, built-in grid), and `--kalman-physics-candidate-learning-rate` (default `0`, built-in grid); `optimize-equity` can sample those values with matching `--kalman-physics-*-min/max` range flags, preserving the previous latest-1000/train-700/test-300 and measurement-assimilation/model-selection behavior unless overridden. Optimizer records and top-combo payloads persist those fusion settings, allowing prior sampling, correlation analysis, and top-combo adoption to round-trip them.
  - `optimize-equity` can bias random search toward prior top-combo parameter neighborhoods with `--prior-json`, `--prior-sample-prob`, `--prior-method-sample-prob`, `--prior-rank-bias`, `--prior-edge-weight`, `--prior-top-fraction`, and `--prior-min-samples`; eligible prior rows are ranked by the active optimizer objective and ROI penalty config plus a tunable positive-edge boost from annualized return, Sharpe, Calmar, profit factor, walk-forward Sharpe, and activity evidence before sampling, and top-combo `processing.rankScore` is used as a rank floor when present so freshness-aware deployable rows keep their sampling influence. Stderr reports selected-prior edge average/max values so weak prior pools are visible. Prior-guided samples can adopt enabled historical method families, and rank-biased draws favor the strongest eligible priors while preserving tail exploration. `--prior-perturb-scale-double` and `--prior-perturb-scale-int` tune how tightly those prior-guided samples jitter around validated combos, independently of broader survivor mutations, while `--prior-age-half-life-days` discounts older prior rows, `--prior-missing-age-multiplier` controls the decay multiplier for prior rows without `createdAtMs`, `--prior-diversity-max-per-bucket` prioritizes platform/interval/method diversity, and `--prior-seed-count` deterministically evaluates the strongest selected priors at the start of the seed phase. Missing walk-forward summaries on legacy prior rows no longer exclude them from sampling, because fresh trials still have to pass the active walk-forward gates before ranking/deployment. During exploitation, the survivor pool refreshes with newly eligible high-score trials so later perturbation/crossover draws follow the strongest neighborhoods found after the seed phase. Optimizer records and top-combo JSON now include `optimizerTrialOrigin` so results can be compared by `sobol-seed`, `prior-sample`, `prior-method-sample`, `prior-seed`, `survivor-perturb`, or `survivor-crossover` source. `haskell/scripts/run_optimize_equity_top5.sh` forwards these via `PRIOR_JSON`, `PRIOR_SAMPLE_PROB`, `PRIOR_METHOD_SAMPLE_PROB`, `PRIOR_RANK_BIAS`, `PRIOR_EDGE_WEIGHT`, `PRIOR_TOP_FRACTION`, `PRIOR_MIN_SAMPLES`, `PRIOR_PERTURB_SCALE_DOUBLE`, `PRIOR_PERTURB_SCALE_INT`, `PRIOR_AGE_HALF_LIFE_DAYS`, `PRIOR_MISSING_AGE_MULTIPLIER`, `PRIOR_DIVERSITY_MAX_PER_BUCKET`, and `PRIOR_SEED_COUNT`, while server-launched optimizer runs use the current top-combo file by default and expose the same controls as `TRADER_OPTIMIZER_PRIOR_JSON`, `TRADER_OPTIMIZER_PRIOR_SAMPLE_PROB`, `TRADER_OPTIMIZER_PRIOR_METHOD_SAMPLE_PROB`, `TRADER_OPTIMIZER_PRIOR_RANK_BIAS`, `TRADER_OPTIMIZER_PRIOR_EDGE_WEIGHT`, `TRADER_OPTIMIZER_PRIOR_TOP_FRACTION`, `TRADER_OPTIMIZER_PRIOR_MIN_SAMPLES`, `TRADER_OPTIMIZER_PRIOR_PERTURB_SCALE_DOUBLE`, `TRADER_OPTIMIZER_PRIOR_PERTURB_SCALE_INT`, `TRADER_OPTIMIZER_PRIOR_AGE_HALF_LIFE_DAYS`, `TRADER_OPTIMIZER_PRIOR_MISSING_AGE_MULTIPLIER`, `TRADER_OPTIMIZER_PRIOR_DIVERSITY_MAX_PER_BUCKET`, and `TRADER_OPTIMIZER_PRIOR_SEED_COUNT`.
  - The prior/survivor positive-edge boost is component-tunable with `--edge-score-annualized-return-weight`, `--edge-score-sharpe-weight`, `--edge-score-calmar-weight`, `--edge-score-profit-factor-weight`, `--edge-score-walk-forward-sharpe-weight`, `--edge-score-activity-weight`, `--edge-score-calmar-cap`, and `--edge-score-activity-cap`; API optimizer requests accept matching `edgeScore*` camelCase fields, server-launched optimizer runs forward matching `TRADER_OPTIMIZER_EDGE_SCORE_*` env controls, and the top-5 wrapper forwards `EDGE_SCORE_*`. When `--prior-age-half-life-days` is enabled, prior rows without `createdAtMs` use `--prior-missing-age-multiplier` (default `0.25`) instead of being treated as fresh.
  - Optimizer positioning mix is externally tunable with `--p-long-short` (default `0.2`); API optimizer requests and the advanced web optimizer form accept `pLongShort`, server-launched optimizer runs can set `TRADER_OPTIMIZER_P_LONG_SHORT`, and the top-5 wrapper forwards `P_LONG_SHORT`.
  - Cost-aware edge sampling is externally tunable with `--p-cost-aware-edge` (default `1.0`); API optimizer requests and the advanced web optimizer form accept `pCostAwareEdge`, server-launched optimizer runs can set `TRADER_OPTIMIZER_P_COST_AWARE_EDGE`, and the top-5 wrapper forwards `P_COST_AWARE_EDGE`.
  - The top-5 optimizer wrapper forwards `BARS_AUTO_PROB` into `--bars-auto-prob` instead of hardcoding `0`, preserving fixed-bar audits by default while allowing ROI sweeps to reintroduce auto-sized history sampling.
  - LSTM Adam optimizer coefficients are tunable with `--lstm-adam-beta1`, `--lstm-adam-beta2`, and `--lstm-adam-eps` on both `trader-hs` and `optimize-equity`; `optimize-equity` can also sample them with `--lstm-adam-beta1-min/max`, `--lstm-adam-beta2-min/max`, and `--lstm-adam-eps-min/max`, and persists/adopts the sampled values per combo. API optimizer requests accept matching `lstmAdamBeta1Min/Max`, `lstmAdamBeta2Min/Max`, and `lstmAdamEpsMin/Max` range fields plus fixed `lstmAdamBeta1`, `lstmAdamBeta2`, and `lstmAdamEps`; `haskell/scripts/run_optimize_equity_top5.sh` forwards `LSTM_ADAM_*` fixed/range env settings, and server-launched optimizer runs can set fixed/range `TRADER_OPTIMIZER_LSTM_ADAM_*` env controls.
  - Research deploy configs reserve capacity for optimizer discovery by disabling bot autostart while keeping the auto optimizer enabled. Fly research uses `TRADER_OPTIMIZER_TRIALS=18`, `TRADER_OPTIMIZER_TIMEOUT_SEC=480`, `TRADER_OPTIMIZER_LOOKBACK_WINDOWS=7d,14d,30d`, `TRADER_OPTIMIZER_INTERVALS=5m,15m,1h,2h,4h,6h,12h,1d`, `TRADER_OPTIMIZER_MAX_POINTS=5000`, and `TRADER_OPTIMIZER_MAX_COMBOS=1000`; Hetzner research now uses an `8` trial primary pass plus a `12` trial low-threshold discovery-recovery pass, `180` second trial caps, epochs<=8 / hidden<=40, `TRADER_OPTIMIZER_TUNE_MAX_THRESHOLD_CANDIDATES=0`, lower-frequency `2h,4h,6h,12h,1d` scopes, and `TRADER_OPTIMIZER_MAX_POINTS=1000`. `TRADER_OPTIMIZER_AUTO_CAPPED_LOOKBACKS=true` keeps intervals in the pool by deriving the largest safe lookback when all configured windows exceed the fetch cap. Hetzner disables exact prior seeding, lowers prior-biased sampling to `0.25`, samples cost-aware entry on `0.35` of trials, and favors Method 10 with some Method 01 exploration so combo freshness does not depend on manual imports. Fly and Hetzner research also apply `TRADER_TOP_COMBO_SCORE_FRESHNESS_HALF_LIFE_DAYS=30` with floor `0.10` so stale leaderboard rows are discounted without completely masking durable older candidates. Hetzner limits scheduled top-combo backtests to `40` rows every `21600` seconds so refresh work does not starve candidate discovery. Live-gap feedback itself can be tuned through `TRADER_OPTIMIZER_LIVE_GAP_MIN_COMBO_OPERATIONS`, `TRADER_OPTIMIZER_LIVE_GAP_MIN_TOTAL_OPERATIONS`, `TRADER_OPTIMIZER_LIVE_GAP_MULTIPLIER_FLOOR`, and `TRADER_OPTIMIZER_LIVE_GAP_MULTIPLIER_CEILING` without changing code.
  - Optimizer sweeps now normalize generated min/max position sizes before launching a trial, so no-confidence-sizing samples cannot fail the CLI with a zero `--min-position-size`. The sampler also skips disabled method `11` instead of spending trials on a method the backtest CLI rejects.
  - `optimize-equity` now samples the same TA, predictor-training, adaptive performance-gate, blend, signal-gate, regime-selector, and tri-layer price-action shape coefficients with matching `*-min` / `*-max` range flags, including predictor GBDT/conformal calibration constants, `--predictor-tcn-ridge-lambda-min/max`, quantile predictor `--predictor-quantile-epochs-min/max`, `--predictor-quantile-learning-rate-min/max`, and `--predictor-quantile-l2-min/max`, plus KNN/decision-tree/transformer/HMM predictor ranges such as `--predictor-knn-k-min/max`, `--predictor-decision-tree-max-depth-min/max`, `--predictor-transformer-temperature-min/max`, and `--predictor-hmm-iterations-min/max`, adaptive win-rate/profit-factor ramp slack, edge-blend power, anchor-blend neutral-anchor, coherence-gate shrink/boost, entropy-blend alpha, fractal-blend return/gain, phase-cancel return/conflict/alignment, tension-gate shrink coefficients, regime ADX/Aroon/slope/width scoring floors/spans/weights, and tri-layer wick/body/tolerance ratios including `--tri-layer-price-action-body-open-threshold-mult` (default `0.25`) plus matching optimizer min/max flags. It also accepts fixed `--roi-score-*` scorer weights for expectancy, payback, activity, round-trip, and exposure evidence penalties. It stores the sampled values in trial/top-combo JSON, and top-combo adoption hydrates those stored values back into runtime args so parameter-to-ROI analysis can rank and deploy them alongside the existing optimizer knobs.
  - The Sections menu now shows loaded open-position and orphaned-position counts next to Positions and Orphaned ops.
  - Live Bot symbol selectors now put symbols with currently open Binance futures positions first, ahead of flat symbols, while preserving the existing active/running/startup ordering within each group.
  - New Trade forms now default to live armed trading and quote-fraction sizing with `orderQuoteFraction=1`, `orderQuote=0`, and `orderQuantity=0`, matching the Live Bot & Trade preset unless the operator disables live orders, disarms trading, or chooses fixed quote/base quantity sizing.
  - Server-side auto optimization now passes explicit deployability gates and exposes `TRADER_OPTIMIZER_MIN_ROUND_TRIPS`, `TRADER_OPTIMIZER_MIN_EXPOSURE`, `TRADER_OPTIMIZER_MIN_SHARPE`, `TRADER_OPTIMIZER_MIN_CALMAR`, `TRADER_OPTIMIZER_MIN_WF_SHARPE_MEAN`, and `TRADER_OPTIMIZER_MAX_WF_SHARPE_STD`. `TRADER_OPTIMIZER_LOOKBACK_WINDOWS` optionally supplies a comma-separated set of windows; the auto loop samples feasible `(interval, window)` pairs so short intervals can use shorter windows without excluding longer-horizon discovery. When every configured window is too deep for an interval under the current fetch cap, default-on `TRADER_OPTIMIZER_AUTO_CAPPED_LOOKBACKS` derives the largest split-feasible window instead of dropping the interval. The primary pass uses the discovery open/close threshold, hold/cooldown, min-edge, signal-to-noise, and edge-buffer ranges while retaining the primary deployability gates; if a run emits only filtered rows or timeouts, including cost-floor and walk-forward Sharpe failures, the server retries once with discovery recovery: `TRADER_OPTIMIZER_DISCOVERY_RECOVERY_ENABLED`, `TRADER_OPTIMIZER_DISCOVERY_RECOVERY_TRIALS`, `TRADER_OPTIMIZER_DISCOVERY_RECOVERY_TIMEOUT_SEC`, `TRADER_OPTIMIZER_DISCOVERY_MIN_ROUND_TRIPS`, `TRADER_OPTIMIZER_DISCOVERY_MIN_EXPOSURE`, `TRADER_OPTIMIZER_DISCOVERY_MIN_SHARPE`, and `TRADER_OPTIMIZER_DISCOVERY_MIN_CALMAR`. Optimizer JSONL records now include `createdAtMs`, so auto/manual merges carry fresh combo timestamps into newest-first views.
  - Live bot auto-start now defaults to a small restart budget: 3 target bots, 0 top-combo bots during the startup phase, and 1 new worker per poll cycle. `/bot/start` launches at most `TRADER_BOT_START_MAX_SYMBOLS` immediately, then queues any remaining requested symbols and starts them one by one after current bots are stable. Symbols with orphaned futures positions are ordered before requested/env/top-combo targets before those caps and queues are applied. Explicit `botSymbols` / `binanceSymbol` payloads take precedence over `TRADER_BOT_SYMBOLS`; the env list is only the fallback for requests without explicit symbols. Queued starts skip symbols that are already running or starting before waiting on stability, duplicate starts return the existing bot status instead of a 400, and queued starts fail the remaining queue after `TRADER_BOT_START_QUEUE_MAX_WAIT_SEC` seconds unless set to `0` for an unbounded wait. Transient below-threshold order errors no longer block the queue forever, while halted bots and order errors that reach `--max-order-errors` still block. Live starts skip `TRADER_BOT_DISABLED_SYMBOLS` (default `MATICUSDT`) so delisted/closed symbols do not churn the queue. When a start resolves to a top-combo UUID, startup first runs a fresh Binance-backed backtest for that combo; if refreshed ROI is not positive, startup aborts but does not delete the combo. Setting `TRADER_BOT_AUTOSTART=false` disables the default auto-start loop without supervisor restart churn; explicit `/bot/start` requests still work. Tune `TRADER_BOT_TOP_COMBO_BOTS`, `TRADER_BOT_TOP_COMBO_BOTS_STARTUP`, `TRADER_BOT_AUTOSTART_MAX_BOTS`, `TRADER_BOT_AUTOSTART_MAX_STARTS_PER_CYCLE`, `TRADER_BOT_START_MAX_SYMBOLS`, `TRADER_BOT_START_QUEUE_MAX_WAIT_SEC`, `TRADER_BOT_DISABLED_SYMBOLS`, `TRADER_BOT_ONLINE_EPOCHS`, `TRADER_BOT_ONLINE_VAL_RATIO`, `TRADER_BOT_ONLINE_PATIENCE`, `TRADER_BOT_TRAIN_BARS`, `TRADER_BOT_OPTIMIZE_TRAIN_BARS_CAP`, `TRADER_BOT_MAX_POINTS`, `TRADER_BOT_OUTCOME_WEIGHT_WIN_SCALE`, `TRADER_BOT_OUTCOME_WEIGHT_LOSS_SCALE`, `TRADER_BOT_OUTCOME_WEIGHT_CAP`, and `TRADER_BOT_AUTOSTART_BACKOFF_MULTIPLIER` when a deployment has enough memory for wider recovery/adoption, needs different live online-training defaults or outcome-weighted LSTM fine-tune behavior, or wants a different transient retry slope.
  - Local helper startup uses paper/test order mode unless `TRADER_BINANCE_LIVE=true` is set before `haskell/scripts/start_api_bg.sh` launches the API. With live mode and autostart/trading enabled, bot workers can send real Binance orders.
  - State sync uses compact top-combo payloads for outbound `/state/sync` writes, does not synchronously re-post inbound `/state/sync` imports to the configured sync target, and supports `TRADER_STATE_SYNC_TIMEOUT_SEC` (default 15, max 300) for slower cross-deployment sync links.

## Autoloop
- The forever runner's branch reconciliation never auto-resolves merge conflicts. A conflicted merge is aborted (`git merge --abort`) and the branch is left unmerged and logged for operator review; if syncing the base branch with origin itself conflicts — including after a rejected push retry — the cycle halts with `ok: false` before anything else (merges, pushes, merged-ref pruning) can run on the diverged base. The previous policy (keep the base side of each conflicted file) produced semantically torn trees that broke `main` and silently dropped branch work; conflicts now always require a human.
- The bounded auto-loop now polls recent GitHub pull requests for unresolved Copilot/Codex review threads before choosing a new autonomous idea. It asks the planner to validate whether each review is correct, prioritizes valid actionable review fixes, and falls back to the normal improvement selection when no review feedback is worth changing. Set `AUTOLOOP_DISABLE_AI_REVIEW_POLL=1` to disable the review pass, or tune `AUTOLOOP_AI_REVIEW_LOOKBACK_PRS`, `AUTOLOOP_AI_REVIEW_MAX_THREADS`, and `AUTOLOOP_AI_REVIEW_THREAD_MAX_CHARS` to adjust scan breadth and prompt size.
- The repo autoloop now treats merged local branches that are still attached to Git worktrees as prune skips instead of blocking the forever runner before any bounded repair cycle starts.
- The forever runner now prunes stale `git worktree` metadata before merged-ref cleanup, and it saves failed dirty-cycle snapshots onto dedicated `autoloop/recovery/*` or `autoloop/checkpoint/*` branches instead of writing those `WIP` rescue commits directly onto `main`.
- The bounded autoloop now includes `hlint app test bench` in its safe verification set and can apply direct `Found:` to `Perhaps:` HLint replacements for editable Haskell files when CI fails on simple HLint-only suggestions.
- The logical-correctness loop now explicitly starts each cycle by auditing the Haskell trading algorithm for more logical and correct behavior, then implements the selected backend fix with tests, formal notes, and local verification where practical.
- The GitHub-hosted autoloop now requires `AUTOLOOP_PUSH_TOKEN`; it no longer falls back to `github.token`, and it no longer skips post-push CI waiting. That keeps Actions polling and failed-log ingestion mandatory after each autoloop push.
- The GitHub-hosted autoloop now installs a pinned prebuilt `fourmolu` release binary instead of compiling `fourmolu` from source on every run, which removes a slow bootstrap step before the bounded loop can start.

## AI Development Workflow
- Use `bash scripts/verify.sh` as the canonical local verification entry point.
- Repo-root shortcuts:
  - `npm run verify:haskell`
  - `npm run verify:web`
  - `npm run verify:automation`
  - `npm run verify`
- Keep `haskell/web` runtime dependencies `react` and `react-dom` pinned to the exact same version before shipping web changes; patch-level drift can crash the production bundle at startup.
- The Vite dev proxy loads both `haskell/web/.env*` and the repo-root `.env`; repo-root `TRADER_API_TOKEN` takes precedence so direct `npm run dev` matches the local helper scripts.
- Local REST API startup now requires ops persistence. For local development, prefer `bash haskell/scripts/run_api_with_db.sh`; if you run `trader-hs -- --serve` directly, set `TRADER_DB_URL` (or `DATABASE_URL`) first.
- The repo now pins local tool versions with `.tool-versions` and `.nvmrc`.
- Local autoloop and Codex environment toggles are documented in `.env.example`.
- See `docs/ai-workflow.md` for the full bootstrap, verification, and automation workflow.
